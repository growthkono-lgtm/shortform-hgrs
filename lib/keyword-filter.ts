/**
 * 키워드 선별 규칙 + 니치 점수 — 단일 출처. (2026-08-13 저녁)
 *
 * 1차 수집에서 명함제작·로고제작·아크릴제작·기념품제작 같은 것이 상위에 올라왔다.
 * 검색량은 크지만 **우리가 파는 것이 아니다.** 그런 키워드로 글을 쓰면 트래픽은
 * 와도 리드가 안 된다(사장님 지적).
 *
 * 그리고 더 중요한 지적: **검색량이 크다고 좋은 게 아니다.**
 * 우리는 신생 도메인이라 "마케팅"(월 19,800·경쟁 높음) 같은 키워드로는 당장
 * 순위를 못 잡는다. 적정 검색량 + 높은 클릭률 + 할 만한 경쟁인 **니치 키워드**를
 * 주력으로 두고, 큰 키워드는 장기전으로 섞는다.
 */

/**
 * 우리 서비스 축 — 이 중 하나에 걸려야 우리 키워드다.
 *
 * 축을 "마케팅 전반"이 아니라 **우리가 실제로 파는 것**으로 좁혔다.
 * 콘텐츠·숏폼·영상·AI·브랜딩·SNS·인플루언서·퍼포먼스가 본체이고,
 * 커머스·CRM·IMC 는 그 결과를 매출로 잇는 인접 축이다.
 */
export const SERVICE_AXIS =
  /숏폼|쇼츠|릴스|영상|비디오|촬영|편집|모션|콘텐츠|컨텐츠|소재|크리에이티브|브랜딩|브랜드|SNS|인스타|유튜브|틱톡|채널운영|인플루언서|크리에이터|체험단|서포터즈|앰버서더|바이럴|퍼포먼스|광고|ROAS|전환|퍼널|리타겟|어트리뷰션|마케팅|그로스|AI|AX|생성형|머신러닝|딥러닝|자사몰|쇼핑몰|이커머스|커머스|카페24|스마트스토어|D2C|상세페이지|랜딩|CRM|프로모션|리텐션|LTV|객단가|재구매|IMC|BTL|팝업|팬덤|캠페인|런칭|포지셔닝|USP|PMF|스타트업|SEO|AEO|상위노출|리뷰|후기|카피|스토리텔링|프로덕트/i;

/**
 * 축에 걸려도 **우리 일이 아닌 것**.
 *
 * "제작"이 들어간다고 다 우리 일이 아니다 — 명함·로고·판넬·아크릴은
 * 인쇄·굿즈 업체의 영역이다. 이런 게 상위에 오면 편성이 통째로 어긋난다.
 */
export const NOT_OURS =
  /명함|로고|아크릴|판넬|POP|현수막|배너제작|간판|기념품|판촉물|패키지제작|조형물|인쇄|스티커|굿즈|홈페이지제작|웹사이트제작|쇼핑몰제작|앱제작|프로그램제작|드라마제작사|영화제작사|음반|웨딩|돌잔치|프로필사진|증명사진|자격증|시험|기사자격|알바|채용|구인|학원|국비|내일배움|공연|연극|전시회|골프|제주|맛집|놀거리|관광|대출|보험|부동산|주식|코인|엔비디아|SQLD|ADSP|키오스크|밀수|관세|수익창출|월급|연봉|취업|자소서|면접|과제|레포트|논문|웹툰|소설|게임|다이어트|병원|한의원|피부과|성형|변호사|세무사|노무|법률|여성의류|남성의류|의류쇼핑몰/i;

/** 소비자가 치는 말 — 브랜드 담당자가 아니다 */
export const CONSUMER_INTENT =
  /추천순위|어디서|가성비|할인|쿠폰받|무료로|공짜|후기좋은|내돈내산|언박싱|먹방|브이로그|성수|홍대|잠실|강남역|팝업일정|팝업정보|입점|정산|환불|배송|반품|고객센터|로그인|다운로드/i;

/**
 * **툴을 직접 쓰는 사람**의 검색어 — 우리 고객이 아니다.
 *
 * 축 필터만 걸었더니 "유튜브음원추출", "영상편집컴퓨터사양", "AI타로",
 * "인플루언서되는법" 이 니치 점수 상위에 올라왔다(2026-08-13 실측).
 * 전부 영상/AI 축에 걸리지만, 치는 사람은 **직접 만들려는 실무자나 개인**이지
 * 맡기려는 브랜드 결정권자가 아니다. 이런 글로 트래픽이 와도 리드가 안 된다.
 */
export const PRACTITIONER_INTENT =
  /되는법|되는방법|만들기|만드는법|배우기|배우는|강의|강좌|자격|입문|초보|independent|사양|스펙|추출|다운|변환|자막생성|무료툴|프로그램추천|앱추천|사이트추천|템플릿|폰트|소스|플러그인|단축키|사용법|쓰는법|튜토리얼|채용|구직|포트폴리오만|연봉|월급|프리랜서되|부업|투잡|수익|구독자수|조회수늘|팔로워늘|계정키우|타로|운세|일기|다이어리/i;

/**
 * **B2B 구매 의도** 신호 — 맡기거나, 예산을 잡거나, 전략을 짜는 사람의 말.
 * 이 중 하나는 걸려야 우리 리드로 이어질 수 있다.
 */
export const BUYER_INTENT =
  /대행|업체|회사|에이전시|비용|견적|단가|가격|요금|제작|외주|의뢰|맡기|전략|기획|방안|사례|성공사례|추천|비교|선정|운영|관리|마케팅|광고|브랜딩|캠페인|매출|전환|ROAS|성과|효과|예산|플랜|컨설팅|솔루션|B2B|기업|브랜드/i;

export type Metrics = {
  /** 검색어 자체. 어절 수로 롱테일 여부를 판정하므로 난이도 계산에 필요하다 */
  term?: string;
  totalVolume: number;
  mobileCtr: number | null;
  pcCtr: number | null;
  competition: string | null;
};

/**
 * 우리 키워드인가 — 네 관문을 다 통과해야 한다.
 *
 *  1. 우리 서비스 축에 걸리는가
 *  2. 우리 일이 아닌 것(명함·로고·인쇄)이 아닌가
 *  3. 소비자 검색어가 아닌가
 *  4. 툴 사용자 검색어가 아닌가 + 구매 의도가 있는가
 *
 * 4번이 마지막에 붙은 이유: 1~3만으로는 "영상편집컴퓨터사양" 같은 게 통과했다.
 * 축은 맞는데 치는 사람이 우리 고객이 아니었다.
 */
/**
 * 업체명(브랜드 검색어) 차단. (2026-08-14)
 *
 * 사장님 지적: *"남의 업체 이름은 왜 들어가 있어 설마."* 키워드 보드 1위가
 * `새결마케팅` 이었다. 실재하는 마케팅 대행사다.
 *
 * 원인은 우리가 아니라 수집원이다 — 네이버 검색광고 키워드도구는 연관 검색어를
 * 뱉을 때 **브랜드 검색어를 같이 준다.** 그걸 그대로 담았다.
 *
 * 이런 검색어는 노려도 소용이 없다. 그 회사 이름을 친 사람은 그 회사를 찾는
 * 것이지 대안을 찾는 게 아니라, 순위를 잡아도 문의로 안 이어진다.
 * (남의 상호로 트래픽을 가져오는 건 별개로 하지 않을 일이기도 하다.)
 *
 * ⚠️ **규칙으로 잡으려다 접었다.** "…마케팅/…광고/…대행사로 끝나는데 앞자리가
 * 일반명사가 아니면 업체명" 규칙을 짜 봤더니 730개 중 149개가 걸렸는데
 * `광고대행사`·`제휴마케팅`·`화장품마케팅` 같은 멀쩡한 검색어가 대부분이었다.
 * 좋은 키워드를 날리는 쪽이 업체명 몇 개를 남기는 쪽보다 비싸다.
 *
 * 그래서 **명시 목록 + 어드민의 [제외] 버튼** 두 가지로 간다.
 * 눈에 걸리는 걸 한 번 누르면 `blog_keyword.status = 'blocked'` 가 되고
 * 보드에서도 편성에서도 빠진다. 판단은 사람이 하고 기억은 DB 가 한다.
 */
const BLOCKED_BRANDS = ["새결마케팅", "올바른마케팅"];

export function isBrandName(term: string): boolean {
  return BLOCKED_BRANDS.includes(term.trim());
}

/**
 * 우리 키워드인가 — 배제 관문 셋을 통과하면 담는다. (2026-08-14 완화)
 *
 * 앞 판은 `BUYER_INTENT` 까지 **필수**로 걸었다. 그래서 21,779개를 수집해도
 * 565개만 남았고, 사장님이 요구한 "니치 200 / 전체 1,000" 에 한참 못 미쳤다.
 *
 * 구매의도를 필수에서 뺀 이유는 퍼널 때문이다. 검색은 인지 → 고려 → 구매로
 * 내려오는데, 구매 단계 검색어만 노리면 **이미 대행사를 찾기로 마음먹은 사람**만
 * 만난다. 그 자리는 경쟁이 가장 세고 우리 도메인이 아직 못 이긴다.
 *
 * 대신 인지·고려 단계 검색어로 먼저 만나서 글로 신뢰를 만들고, 글 안에서
 * 우리 서비스로 착지시킨다. 그게 지금 도메인 힘으로 실제 리드를 만드는 길이다.
 *
 * 구매의도는 버리지 않는다 — `hasBuyerIntent` 로 따로 재서 **니치 점수에
 * 가중치**로 얹는다. 같은 조건이면 돈 되는 검색어가 위로 온다.
 */
export function isOurs(term: string): boolean {
  if (isBrandName(term)) return false;
  return (
    SERVICE_AXIS.test(term) &&
    !NOT_OURS.test(term) &&
    !CONSUMER_INTENT.test(term) &&
    !PRACTITIONER_INTENT.test(term)
  );
}

/** 맡기거나·예산을 잡거나·비교하는 사람의 말인가. 편성 우선순위에 쓴다 */
export function hasBuyerIntent(term: string): boolean {
  return BUYER_INTENT.test(term);
}

const COMP_WEIGHT: Record<string, number> = { 낮음: 1, 중간: 0.6, 높음: 0.25 };

/**
 * 니치 점수 — "지금 우리가 이길 수 있고, 이기면 값이 되는" 정도. 0~100.
 *
 * 세 축을 곱한다:
 *  · 검색량 — 크면 좋지만 **로그**로 눌러 담는다. 월 20,000 이 월 2,000 보다
 *    10배 좋은 게 아니다. 클수록 경쟁도 세지기 때문이다.
 *  · 클릭률 — 검색 결과에서 실제로 눌리는가. 광고 CTR 이지만 그 검색어에
 *    **행동 의사가 있는지**의 대리 지표로는 쓸 만하다.
 *  · 경쟁도 — 낮을수록 신생 도메인이 비집고 들어갈 자리가 있다.
 *
 * 곱셈인 이유: 하나가 0 에 가까우면 나머지가 커도 소용없다. 검색량이 커도
 * 경쟁이 '높음'이면 지금은 못 이기고, 경쟁이 낮아도 아무도 안 치면 값이 없다.
 */
export function nicheScore(m: Metrics): number {
  const volume = Math.min(1, Math.log10(Math.max(m.totalVolume, 1)) / 4); // 10,000 에서 1.0
  const ctr = Math.min(1, (m.mobileCtr ?? m.pcCtr ?? 0) / 2); // 2% 면 만점
  const comp = COMP_WEIGHT[m.competition ?? ""] ?? 0.4;

  // 클릭률은 0 인 키워드가 많다(광고가 안 붙은 검색어). 그걸 0 점으로 만들면
  // 정보성 키워드가 통째로 사라지므로 바닥을 깔아 준다
  const ctrFloor = Math.max(ctr, 0.25);

  return Math.round(volume * ctrFloor * comp * 100);
}

/**
 * 공략 난이도 — 편성에서 요일별로 섞는 기준.
 *
 * 사장님 지시: "어떤 날은 빡센 경쟁 메인 큰 키워드, 어떤 날은 적당한 검색량
 * 적당한 클릭률 키워드 등 섞어야 SEO/AEO 전략을 쓰는 것이다."
 */
export type Difficulty = "니치" | "중간" | "빅";

/**
 * SEO 난이도 판정 — 2026-08-14 전면 재설계.
 *
 * ── 앞 판이 틀린 이유 ─────────────────────────────────────────────────
 * 네이버가 주는 `competition`(경쟁정도)을 난이도로 썼다. 그런데 그 값은
 * **광고 입찰 경쟁**이지 검색 순위 경쟁이 아니다. 마케팅 계열 검색어는
 * 대행사들이 전부 광고를 돌리기 때문에 **725개 중 656개가 '높음'** 으로 나왔다.
 * 그 결과 난이도가 한 칸에 뭉쳐 요일별로 섞을 재료가 안 됐다(니치 49개뿐).
 *
 * ── 대신 무엇을 보는가 ────────────────────────────────────────────────
 * SEO 난이도의 실제 대리 지표는 두 개다.
 *  1. **검색량** — 클수록 그 자리에 오래된 도메인이 앉아 있다
 *  2. **어절 수** — 검색어가 길수록(롱테일) 의도가 좁고 경쟁 문서가 적다
 *
 * 경쟁정도는 버리지 않고 **구매의도 신호**로만 쓴다 — 광고비를 태우는
 * 검색어라는 건 돈이 오가는 자리라는 뜻이라, 오히려 반가운 신호다.
 *
 * 하한을 300 으로 둔 건 사장님 기준이다: *"검색량이 적어도 최소 1-2천 건
 * 이상은 나오는 게 맞아. 그 이하는 정말 별 의미가 없어서."* 다만 신생
 * 도메인이 1,000+ 를 바로 잡을 수는 없어서, 니치 상한을 1,500 으로 두고
 * 300 미만은 아예 편성에서 뺀다.
 */
export function difficultyOf(m: Metrics): Difficulty {
  const words = m.term ? m.term.trim().split(/\s+/).length : 1;
  const longTail = words >= 3 || (m.term?.length ?? 0) >= 12;

  if (m.totalVolume >= 5000) return "빅";
  if (m.totalVolume >= 2000) return longTail ? "중간" : "빅";
  if (m.totalVolume >= 1000) return longTail ? "니치" : "중간";
  return "니치";
}

/**
 * 요일별 난이도 배분 — 월·수·목.
 *
 * 빅 키워드만 노리면 6개월 동안 아무 순위도 못 잡고,
 * 니치만 노리면 트래픽 천장이 낮다. 섞는다.
 */
/**
 * 요일별로 어떤 난이도를 노릴 것인가. (2026-08-14 전면 재조정)
 *
 * ── 왜 니치가 다수인가 ────────────────────────────────────────────────
 * hgrs.io 는 2025-05 등록으로 지금 1년 3개월이다. 신규 유예는 지났지만,
 * 검색량 1,000 이상 구간은 **사실상 전부 경쟁 '높음'** 이고 그 자리엔 5~10년 된
 * 대행사 사이트와 네이버 블로그가 앉아 있다. 그걸 밀어내는 데 6~12개월이 든다.
 *
 * 반면 경쟁이 낮은 니치는 2~8주면 1페이지에 붙는다. 순서가 중요하다 —
 * **작은 걸로 먼저 이겨서 도메인 신뢰를 쌓고, 그 힘으로 큰 걸 친다.**
 * 처음부터 큰 것만 치면 1년 동안 0건이고, 그러면 중간에 접게 된다.
 *
 * 그래서 주 5편은 니치, 주 2편(주말)은 빅으로 간다. 주말에 빅을 두는 건
 * 검색량이 낮은 날이라 순위 경쟁의 손해가 가장 적기 때문이다.
 */
export const DIFFICULTY_BY_WEEKDAY: Record<number, Difficulty> = {
  0: "빅", // 일 — 장기전
  1: "니치", // 월
  2: "니치", // 화
  3: "니치", // 수
  4: "니치", // 목
  5: "니치", // 금
  6: "빅", // 토 — 장기전
};
