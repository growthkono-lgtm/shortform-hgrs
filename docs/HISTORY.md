# 작업 히스토리

프로젝트: **HGRS 숏폼 부스팅** (`shortform.hgrs.io`)
레포: https://github.com/growthkono-lgtm/shortform-hgrs

---

## 2026-08-05 — 1일차

### 만든 것

| 영역 | 상태 |
| --- | --- |
| Next.js 16 / React 19 / Tailwind v4 스캐폴딩 | 완료 |
| 디자인 토큰 (hgrs.io 톤 계승) | 완료 |
| 랜딩 S1~S15 전 섹션 | 완료 — 빈 박스 0개 |
| Supabase 스키마 10테이블 + RLS | 완료 |
| Auth (가입·로그인·포털 가드) | 완료 |
| 토스페이먼츠 결제 플로우 | 완료 (테스트 키) |
| 브랜드 온보딩 + 그로스 AI | 완료 |
| 약관·개인정보·환불규정 | 초안 완료 |
| 채널톡 위젯 | 코드 완료 (키 대기) |
| 실제 산출물 자산 80개 반영 | 완료 |
| **배포** | **보류** — 섹션별 수정 후 |
| **대시보드·어드민** | **미착수** |

### 흐름

랜딩 → 가입 → 브랜드 등록(AI 분석) → 플랜 선택 → 결제 → 프로젝트 자동 생성.
여기까지 한 줄로 이어짐.

---

## 판단 근거가 필요한 결정들

### 디자인 — 액센트는 `#B88C8C`

hgrs.io 라이브 CSS를 긁어보니 눈에 띄는 `#0099FF`는 **Framer 기본 링크 색**이었다.
실제 포인트 컬러는 1·2·3 넘버링 카드의 숫자에 쓰인 **`#B88C8C`**(뮤트된 더스티 로즈).
스펙 S8이 "hgrs.io 1·2·3 패턴"을 지목한 바로 그 요소라 이걸 `--accent`로 잡았다.
파란색은 사용 금지로 주석에 박아둠.

추출한 실측값: 텍스트 `#030303` / 보조 `#595959` / 섹션 배경 `#F7F7F7` /
헤드라인 자간 `-0.04em` / 영문 아이브로우 `+0.02em`.
서체는 라틴 Inter, 한글 **펴진고딕**(Pretendard 기반, SIL OFL — 자체 호스팅).

### 그로스 AI — 스펙의 sonnet-4-6 대신 opus-5, effort=medium

브랜드당 1회만 도는 호출이라 비용 영향이 작고, 여기서 뽑은 USP·타겟이
이후 모든 캠페인 기획의 입력이 된다.
effort는 기본값 `high`에서 **178초**가 걸려 온보딩 UX가 무너졌다.
`medium`으로 낮추니 **45초**, 품질은 유지(USP 3개·금지표현 5개 동일 수준, 헤드라인은 오히려 더 날카로움).

### 결제 — 금액은 서버가 `plans`에서 읽는다

클라이언트가 보낸 금액은 받지도 않는다. 리다이렉트로 돌아온 금액은 대조용.
실제로 4가지 공격 경로를 때려서 확인했다.

| 시나리오 | 결과 |
| --- | --- |
| 금액 조작 (₩2,200,000 → ₩1,000) | `AMOUNT_MISMATCH` 차단 |
| 존재하지 않는 주문 | 차단 |
| 파라미터 누락 | 차단 |
| 정상 금액 + 위조 결제키 | 토스가 `NOT_FOUND_PAYMENT_SESSION` 반환 |

마지막 항목이 시크릿 키 인증이 정상이라는 증거(키가 틀리면 `UNAUTHORIZED_KEY`).

### RLS — `private.is_admin()`에 EXECUTE를 돌려줘야 했다

보안 가이드대로 EXECUTE를 회수했더니 **본인 프로필 조회까지 실패**했다.
RLS 정책은 쿼리를 던진 롤의 권한으로 평가되기 때문.
`authenticated`에만 돌려주고 `anon`은 차단. `private` 스키마는 Data API 미노출이라 외부 호출 경로는 없다.

격리 검증 완료: 사용자1/2가 서로의 행을 못 보고, 비로그인은 `profiles` 빈 배열,
`orders` 직접 INSERT는 `42501`로 차단.

### 자산 — 파일명을 믿으면 안 됐다

Drive 영상 8개를 하나씩 열어보니 파일명과 내용이 달랐다.

| 파일명 | 실제 |
| --- | --- |
| `tpu 비교.mp4` | 비교 영상 아님 → 앞은 **후기형**(평점 4.92), 뒤는 **비교형**("어떤 매트든") |
| `제로블럭 인터뷰` | 인터뷰 아님 → **비포애프터형** |
| `0401_흰색배경` | **제품 실험형** |

파일명대로 매핑했으면 포맷 메뉴판 4칸이 전부 틀렸을 것.
`consultant-sgh`의 `production` 6장도 "촬영 현장"이 아니라 **SEO 구조도·검색결과·서치콘솔 화면**이었다.

### 성과 섹션(S5)은 구조를 바꿨다

ROAS/CTR/CPA 원본 지표가 없어서, 후기에 담긴 **브랜드가 직접 밝힌 결과**로 재구성.
화면에 "브랜드가 전해온 결과, 우리가 산출한 추정치 아님"을 명시.
지표 데이터가 오면 원래 구조(전후 그래프)로 되돌리면 된다.

### 마퀴 타일은 `next/image`를 쓰지 않는다

트랙을 두 벌 이어붙이므로 60장 × 2 = **120건의 런타임 최적화 요청**이 생겨 첫 렌더가 막혔다.
이미 webp로 압축된 자산이라 plain `<img loading="lazy">`로 충분하다.

---

## 보안 점검 (2026-08-05)

| 항목 | 결과 |
| --- | --- |
| git 추적 env 파일 | `.env.example`(빈 템플릿)만 |
| git 히스토리 시크릿 (13커밋 전수) | `sk-ant-` / `sb_secret` / `test_gsk_` / service_role 전부 **0건** |
| 클라이언트 번들 시크릿 | 실제 키 **0건**. `sb_secret` 매치는 Supabase SDK의 접두사 검사 코드(오탐) |
| 브라우저 노출 env | `NEXT_PUBLIC_*` 5개만 (URL·anon키·사이트URL·토스클라이언트키·채널톡키 — 전부 공개 전제) |
| service_role 사용처 | `lib/supabase/server.ts` 서버 전용 1곳 |
| RLS 미적용 테이블 | **0개** (10/10 적용) |
| RLS 격리 | 사용자 간 교차 조회 차단 확인, 직접 INSERT `42501` 차단 |
| SECURITY DEFINER 함수 | `private.is_admin`(비노출 스키마) / `public.handle_new_user`(트리거 전용, EXECUTE 회수 완료 — 회수 후 가입 트리거 정상 동작 재확인) |
| SSRF | `lib/fetch-page.ts` 사설대역·메타데이터 엔드포인트 차단 + 15초 타임아웃 |
| 결제 금액 위변조 | 서버 재검증으로 차단 (위 표) |
| 영상 원본 유출 | 워터마크 미리보기는 미구현(W2~3). 원본은 Storage 공개 경로 금지 원칙만 코드 주석에 명시 |

### 런칭 전 반드시 처리 (보안·운영)

1. **인증 메일 SMTP 미설정** — 지금은 Supabase 내장 발송(시간당 2건, 조직 멤버 주소만).
   이대로 열면 **실제 고객이 가입 인증 메일을 못 받는다.** Resend SMTP 연결 필요.
2. **Site URL이 `localhost:3000`** — 배포 시 인증 메일 링크가 localhost로 감.
3. **비밀번호 최소 길이** — 폼은 8자로 막지만 Supabase 설정은 6자라 API 직접 호출 시 통과.
4. **개발용 테스트 계정 2개** (`dev-test@`, `dev-test2@hgrs-boost.local`) — 런칭 전 삭제.
5. **리전이 싱가포르**(`ap-southeast-1`) — 국내 기준 왕복 60~80ms 추가. 데이터 없는 지금이 이전 비용 최저.

---

## 비용 (2026-08-05 기준)

### 지금 나가고 있는 것

| 항목 | 금액 | 비고 |
| --- | --- | --- |
| **Supabase Pro** | **$25/월** | 조직이 이미 Pro 플랜. 현재 DB 11MB로 여유 큼 |
| Anthropic API | 종량 | 아래 실측 참고 |
| GitHub | $0 | private 레포, 무료 한도 |

### 실측 — 그로스 AI 브랜드 분석 1회

```
input   3,236 tok
output  1,479 tok
비용    $0.0532  (약 74원)
```

opus-5 / effort=medium / hgrs.io 페이지 기준.
**브랜드당 1회만 도는 호출**이므로 월 100개 브랜드가 등록해도 약 **7,400원**.

> 참고: effort를 `high`로 되돌리면 출력 토큰이 크게 늘어 비용도 3~4배가 된다.
> 속도(178초→45초)와 비용 양쪽에서 `medium`이 맞는 선택이었다.

### 아직 안 나가는 것 (연동 전)

| 항목 | 예상 | 조건 |
| --- | --- | --- |
| Vercel | $0 ~ $20/월 | 미배포. Hobby로 시작 가능하나 상업용은 Pro 권장 |
| 토스페이먼츠 | 결제액의 약 2~3% | 현재 공개 테스트 키. 전자결제 신청 후 계약 요율 적용 |
| Resend | $0 ~ $20/월 | 미연동. 무료 3,000통/월 |
| 채널톡 | $0 ~ | 미연동. 무료 플랜 존재 |
| 인스타 스크래핑 벤더 | 미정 | W2에 벤더 선정 |
| Google Drive API | $0 | 서비스 계정, 무료 |

### 비용에 영향이 큰 결정 두 가지

1. **effort=medium 유지** — high로 올리면 AI 비용 3~4배.
2. **영상은 자체 호스팅** — `public/portfolio` 35MB. Vercel 대역폭에 잡히므로
   트래픽이 커지면 CDN/Blob 분리 검토 필요.

---

## 미확정 (사장님 확인 대기)

### 즉시 필요

- **통신판매업 신고번호** — 없으면 PG 심사 자체가 막힘
- **환불규정 문안 확정** — 단계별 환불율 초안은 작성됨. 법률 검토 권장
- **대표자명 · 유선번호 · 개인정보관리책임자** — 푸터 PG 심사 요건
- **브랜드명 게시 동의 범위** — 제로블럭·트러스티랩스·모엔·핏플렉스·고초대졸닷컴·내친소

### 데이터

- 정가 6개 확정 (현재는 임시 제안값)
- 스케일 티어 게시 여부 (챌린지비 실단가 재검산)
- 목표 달성률 90%의 **산출 기준** — 현재 "자체 집계 기준"으로만 표기
- 성과 케이스 원본 지표(ROAS/CTR/CPA)와 그 성과를 낸 소재

### 키

- 채널톡 `pluginKey`
- 토스 전자결제 신청 → 실키 (현재 공개 테스트 키)
- Resend API 키 (Supabase SMTP 연결용)

---

## 다음 순서 (합의됨)

1. 사장님이 **섹션별 수정안** 정리해서 전달 → 반영
2. **배포** (Vercel + `shortform.hgrs.io` DNS CNAME)
3. **대시보드 · 어드민** 착수 (PART E3 / F10)

---

## 자산 위치

| 경로 | 내용 |
| --- | --- |
| `~/Documents/hgrs-boost/public/portfolio/` | 웹용 자산 35MB (레포 포함) |
| `~/Documents/hgrs-assets/raw/` | Drive 원본 영상 8개 523MB |
| `~/Documents/hgrs-assets/web/`, `poster/` | 압축본·포스터 |
| `~/Documents/hgrs-assets/drive-full/` | Drive 폴더 전체 덤프 |
| `~/Documents/hgrs-assets/*.zip` | Drive 원본 zip 584MB |
| `~/Documents/consultant-sgh/public/img/content/` | sgh2 자산 원본 |

원본은 전부 gitignore. GitHub 100MB 파일 제한 때문.

### 유용한 것

- **Drive 다운로드는 `authuser=1`이 필요하다.** 해당 폴더가 `/u/1/` 계정 소속이라
  기본 계정(`authuser=0`)으로 요청하면 403이 난다.
  `https://drive.usercontent.google.com/download?id=<ID>&export=download&authuser=1&confirm=t`
- ffmpeg는 `node_modules/ffmpeg-static/ffmpeg` (이 맥에 Homebrew 없음)
- 인코딩 스크립트: `~/Documents/hgrs-assets/encode.sh`

---

## 2026-08-06 — 2일차 (피그마 개편안 전면 반영)

**이 날의 기준은 하나다: 피그마 `sMAAIUXRna9tduBpfqVzAH` 의 오른쪽 열(신규 와이어프레임).**
왼쪽 열은 1일차 결과물 캡처 + 수정 지시다. 와이어프레임에 없으면 싣지 않는다.

### 삭제한 섹션

와이어프레임에 없어서 통째로 뺐다. 되살리려면 파일이 아니라 와이어프레임을 먼저 고칠 것.

| 섹션 | 원래 내용 |
| --- | --- |
| S2 Market | 76.1% / 10→35% / 5.7% — 피그마에 "해당 섹션 삭제" 명시 |
| S7 Formats | 성과가 검증된 전환 포맷으로만 제작합니다 |
| S8 WhyHgrs | 숏폼 외주가 아니라, 전략 집단의 시스템입니다 |
| S9 Process | 결제 다음 날부터, 무엇이 언제 오는지 보입니다 |
| S10 ForYou | 이런 국면에 있다면, 맞습니다 |

### 최종 섹션 구성

Hero(다크) → 1·2·3 스텝(라이트/인디고/다크 교차) → 서비스 3분할 + 클라이언트 로고
→ 성장사례 1~4(검정) → 크리에이티브 월 → 리뷰 → 플랜 안내 → FAQ → 최종 CTA

### 서비스명·도메인

`HGRS 숏폼 부스팅` → **`스케일업 숏폼 스튜디오`**. `lib/constants.ts` 한 곳만 고치면
헤더·페이지 타이틀·결제 상품명(`planOrderName`)까지 따라간다.
서브도메인은 `shortform` 고정 — 폴더명이 `hgrs-boost`라 헷갈리지만 boost는 쓰지 않는다.

---

## 판단 근거가 필요한 결정들 (2일차)

### 색 — 1일차 결론이 틀렸다

1일차에 "hgrs.io의 `#0099FF`는 Framer 기본 링크색이라 브랜드색이 아니다"까지는 맞았는데,
거기서 **"그러니 블루는 브랜드색이 아니다"로 잘못 결론냈다.**
실제 hgrs.io 히어로에는 두 곡선이 교차한다 — **인디고 `#4D5FE8`**(Project Story 풀블록 패널)과
**골드탄 `#B89B8D`**. 이 둘이 브랜드 2색이다.

역할을 나눴다. 섞어 쓰면 탁해진다.

| 색 | 역할 |
| --- | --- |
| 인디고 `#4D5FE8` / `#3948B8` | CTA·넘버링·강조·표 하이라이트 — **전경 전담** |
| 골드탄 `#B89B8D` / `#8E6B68` | 아이브로우, 섹션 교차 배경(`#f7f5f3`), 히어로 곡선 — **배경 전담** |

골드를 전경 강조색(금액·뱃지)으로 써봤다가 탁해져서 되돌렸다.

### 서체 — Inter가 아니라 DM Sans

hgrs.io 실측(`document.fonts` + computed style): 한글 **Pyeojin Gothic**, 라틴 **DM Sans**.
1일차엔 라틴을 Inter로 잡아 뒀는데 hgrs.io는 Inter를 안 쓴다.

### 한글 줄바꿈 — `word-break: keep-all` 은 필수다

브라우저 기본값은 "요즘 브랜드의 필수 요 / 소," 처럼 **단어 한가운데를 끊는다.**
전역으로 `keep-all` + 본문에 `text-wrap: pretty`를 걸었다. 취향 문제가 아니다.

### 성장 사례 — 사례당 소재는 정확히 하나

사장님이 브랜드별로 직접 지정했다. 여백이 남아 같은 브랜드 소재를 한 편만 더 붙였고,
**다른 브랜드 소재는 절대 섞지 않는다** — 그 소재가 낸 성과처럼 읽힌다.
사례↔성과문구 대응은 피그마 캔버스 Y좌표로 확인했다(추정 아님).

| 사례 | 소재 | 성과 |
| --- | --- | --- |
| 뤼이드 리얼 아카데미 (투자 2,000억) | 아웃풋레포트 + 맘카페 | 3주 연속 고지출 & CPA 견인 |
| 파크론 제로블럭 (매출 200억대) | tpu 비교 + 제로블럭 | 메타 예산 5배 증액, CPA 9만원 절감 |
| 이노바인코리아 모에브 (매출 300억대) | 모에브 컷 1장 | 3개월 내 매출 4천만원 · ROAS 3배 |
| 크래프톤 배틀그라운드 (글로벌) | 조나단 스크린샷 + PNC | 대표이사·협찬 연예인 바이럴 쇼츠 |

크래프톤 대표 소재는 원본이 유튜브 쇼츠(`BdGKoiPITZ0`)인데 저작권 문제로 영상을 싣지 않고
정지 스크린샷만 쓴다. 플레이어 UI가 찍혀 있어 상단 175px을 잘라냈다.

### 파일명이 내용과 다른 것 세 개

첫 프레임을 직접 열어보고 바로잡았다. 파일명만 믿고 배치하면 안 된다.

| 원본 | 실제 내용 |
| --- | --- |
| `맘카페_세로형.mov` | 맘카페 일반 소재가 아니라 **뤼이드 리얼 아카데미** |
| `40대여성.mp4` | 탈모가 아니라 **뼈 건강** (`bone-w40s`) |
| `빌고빌었는데.mp4` | 탈모가 아니라 **펫 사료** (`pet-treats-plea`) |

### 3분할 카드는 소재 영상이 아니라 어드민 화면이다

와이어프레임이 이 자리에 "참고 이미지"로 어드민 캡처를 붙여 뒀다. 앞으로 만들 대시보드의
예고편이다. 캡처를 잘라 넣지 않고 컴포넌트로 그렸다(`service-previews.tsx`) —
실제 어드민이 생기면 목업을 진짜 화면으로 갈아끼우면 된다.

폴더·프로젝트 제목은 **실제 브랜드명을 노출하지 않는다.**
`A브랜드_그로스플랜_7월1차`, `인스타그램_생활_인플_1차_10명` 처럼 플랜명이 들어가는 규칙.

---

## 로고 33개 — 여기서 제일 많이 헤맸다

hgrs.io가 서빙하는 원본 PNG **24개** + 사장님이 준 로고 월 캡처에서 잘라낸 **9개**
(`moev · walla · PARKLON · Riiid · GCAR · 내친소 · ResQ · re: · 열다`).
그 9개는 hgrs.io 51개 페이지 이미지 410개를 전부 긁어도 개별 파일이 없다.

### 캡처 원본은 `~/.claude/image-cache/<세션ID>/` 에 있다

대화에 붙여넣은 이미지를 Claude Code가 원본 PNG로 저장해 둔다.
데스크톱·`/var/folders` 는 macOS TCC가 셸 접근을 막아 못 읽지만 이 캐시는 읽힌다.
**한참 헤맨 지점이니 다음에 캡처를 받으면 여기부터 볼 것.**

### 크기를 CSS `scale` 로 맞추지 말 것

로고마다 눈대중 배율을 주면 화면 폭이 바뀔 때마다 다시 어긋난다.
**파일 자체를 규격화**했다 — `scratchpad/normalize2.py`:
잉크 경계를 픽셀로 재서 여백 제거 → 가로:세로 비율대로 광학 크기 정렬 → 440x110 동일 캔버스.
그 다음 CSS에서 배율을 하나도 주지 않는다.

### ffmpeg 함정 세 개 (전부 여기서 밟았다)

1. **`cropdetect` 가 흰 배경 캡처본에서 안 먹는다.** 투명 배경 로고와 조건이 달라서.
   → 알파+밝기로 직접 판정 (`ink_bbox`)
2. **`color=white` lavfi 합성은 홀수 높이에서 한 줄을 잘라먹는다** (yuv420p 크로마 서브샘플링).
   1024x169 로고가 172032 바이트로 나와 계산이 어긋났다. → 합성 없이 RGBA raw 직접 읽기
3. **`format=rgba` 를 필터 체인 맨 앞에 안 두면 알파가 날아간다.**
   투명 배경이 불투명 회색 사각형으로 구워져 화면에 그대로 나갔다.
   출력에도 `-pix_fmt rgba` 를 고정할 것.

---

## 영상 자산

원본 21편을 720p H.264(crf 30 / AAC 64k mono / faststart)로 인코딩 → `public/portfolio/clips/`.
편당 0.5~3.9MB. 포스터는 **1.0초 지점**에서 뽑는다(첫 프레임은 검은 화면인 경우가 많다).

- 성장사례 2편 + 스텝 1편을 뺀 **18편이 크리에이티브 월**에 흐른다
- 유튜브 12개는 `i.ytimg.com` 썸네일을 직결한다 — 파일로 내려받아 두면 원본 교체 때 어긋난다
- 인코딩 스크립트: `scratchpad/encode.sh`, `encode2.sh`

---

## 보안 점검 (2일차)

| 항목 | 결과 |
| --- | --- |
| `.env*` git 추적 | 차단됨 (`.env.example`만 커밋) |
| 코드 내 하드코딩 시크릿 | 없음 |
| 서버 전용 키(`SERVICE_ROLE`·`TOSS_SECRET`·`ANTHROPIC_API`) | `lib/toss.ts`·`lib/growth-ai.ts`·`lib/supabase/server.ts` 세 곳뿐, **`"use client"` 파일 참조 0건** |
| `NEXT_PUBLIC_*` 노출 변수 | SITE_URL / SUPABASE_URL / SUPABASE_ANON_KEY / TOSS_CLIENT_KEY / CHANNEL_TALK_KEY — 전부 공개 전제 값 |
| 결제 금액 검증 | `app/api/payments/confirm/route.ts` — **DB `orders.amount` 로 승인**하고 쿼리스트링 amount는 대조만. 정상 |

### 이번에 막은 것

원본 영상 24개(**1.6GB**)가 레포 루트에 untracked 로 있었다. `.gitignore` 에는 주석만 있고
실제 패턴이 없어서 그대로 커밋될 뻔했다. `/*.mp4`, `/*.mov`, `/영상모음*`, `/스크린샷*.png`,
`public/logos/_raw/` 를 추가해 차단했다.

## 과금 점검

| 항목 | 수치 | 판단 |
| --- | --- | --- |
| `public/` 총량 | 85MB | Vercel Hobby 제한 내 |
| 영상 38편 | 71MB | 전부 `LoopVideo` 로 **뷰포트 진입 시에만 로드**. 첫 화면 부담 없음 |
| 로고 33개 | 624KB | — |
| 레포에서 뺀 원본 | 1.6GB | 커밋됐으면 클론·빌드 대역폭을 그대로 태울 뻔했다 |

가장 큰 잠재 비용은 크리에이티브 월(18편 자동재생)이다. IntersectionObserver 로
화면 밖에서는 `<video>` 를 아예 만들지 않게 해 뒀다(`components/ui/loop-video.tsx`).

---

## 다음 (3일차)

- **어드민 대시보드** — 3분할 카드의 목업(`service-previews.tsx`)이 그대로 스펙이다.
  인플루언서 선정 보드 / 소스 폴더 모음 / 납품 폴더(1차 수정 요청·최종 확인)
- **가입·결제 스펙** 마무리
- 모바일 실측 (타이포·여백은 미리 손봐 뒀지만 실기기 확인 전)

---

## 2026-08-07 — 3일차 · 랜딩 보강 (수정기획안 v1)

배포: `shortform-hgrs.vercel.app` (프로덕션 2회, 라이브 검증 완료) · 커밋 `2734513`

### 왜 손댔나

랜딩이 "편수를 싸게 파는 숏폼 외주처"로만 읽혔다. 실제로는 브랜드를 통으로 맡아
기획~운영까지 수행하던 프로젝트 팀이 그중 제작 시스템만 잘라 연 것인데,
그 사실이 화면 어디에도 없었다. 성과 사례도 "이 패키지를 사면 나는 결과"로
오독될 여지가 있었다 — 검증 단계에서 제일 먼저 깨지는 대목이다.

축은 하나다: **풀 프로젝트를 하던 팀이 숏폼 소재 시스템만 린하게 열었다.**

### 반영 (기획안 S1·S2·S4·S5·S6·S7)

| 항목 | 내용 |
| --- | --- |
| S7 카운트업 | 초기 DOM에 최종값. JS 없는 환경에서 0으로 노출되던 문제 해결 |
| S1 히어로 | 혈통 한 줄 + 숫자칩 3개 (30+ / 3억대 / ₩2,000만) |
| S2 System 섹션 신규 (`#system`) | 왜 이 가격에 이 퀄리티가 가능한가. Service ↔ Clients 사이 |
| S4 Growth Case | 성과 출처 안내 1줄 + 조건 라벨 4개 교체 |
| S5 Beyond 밴드 신규 (`#beyond`) | 브랜드 단위 프로젝트 수요를 본진으로. Pricing ↔ FAQ 사이 |
| S6 FAQ | "기존 프로젝트와 이 패키지는 어떻게 다른가요" 1문항 |

---

### 카운트업이 크롤러에 0으로 나가고 있었다 (S7)

`GrowthMeter`·`RoasCounter` 둘 다 `useState(0)`(RoasCounter는 첫 단계 `"10"`)에서
시작해 화면 진입 시 목표치까지 올리는 구조였다. 그래서 **JS가 안 도는 환경**
— 검색 크롤러, 카카오·슬랙 링크 미리보기, 일부 인앱브라우저 — 에서 성과가 전부

```
0주  ▼0위  0배  ₩0만  ▼0만원      ROAS 10
```

으로 노출됐다. 성과 섹션이 통째로 0인 페이지가 링크로 돌아다닌 셈이다.

**고친 방식** — 진행도 `p`의 초기값을 `1`(=최종값)로 두고, 애니메이션을
progressive enhancement로만 붙인다. 공통 규칙은 `components/ui/use-count-up.ts`
한 곳에 있고 `GrowthMeter`·`RoasCounter`·히어로 숫자칩이 공유한다.

#### 여기서 한 번 갈렸던 판단 — `useLayoutEffect` 로 0으로 되돌리면 되지 않나

안 된다. **초기 HTML은 하이드레이션보다 먼저 페인트된다.** 최종값을 그려 놓고
하이드레이션 후에 0으로 되돌리면, 사용자 눈에는

> `30+` 이 떴다가 → `0` 으로 튀고 → 다시 올라간다

로 보인다. 하이드레이션이 느린 모바일에서 이 간격이 그대로 노출된다.
`useLayoutEffect`는 **그 커밋의** 페인트만 막아 줄 뿐, 이미 나간 첫 페인트는 못 되돌린다.

그래서 규칙을 이렇게 잡았다:

| 상황 | 동작 |
| --- | --- |
| 마운트 시점에 **이미 화면 안** (히어로 숫자칩) | 애니메이션 걸지 않음. 최종값 그대로 |
| 마운트 시점에 **화면 밖** (성장 사례 대부분) | 0으로 되돌린 뒤 스크롤 진입 시 카운트업 |
| `prefers-reduced-motion` | 최종값 그대로 |

화면 밖에서 0으로 되돌리는 건 눈에 안 보이므로 깜빡임이 없다.
**트레이드오프**: 히어로 숫자칩은 실제로 카운트업이 돌지 않는다(정적 렌더).
기획안은 카운트업을 요청했지만, 하드 요구사항(JS 없이 최종값 노출)과 충돌할 때는
후자를 택했다. 히어로에서 숫자가 튀는 것보다 가만히 있는 편이 낫다.

#### 검증

```
curl -s https://shortform-hgrs.vercel.app/ | grep -o '3주\|▼1위\|5배\|▼9만원\|₩4,000만\|300+'
→ 3주 ▼1위 5배 ▼9만원 ₩4,000만 300+   (전부 HTML 텍스트에 포함)
```

브라우저에서는 스크롤 진입 시 카운트업이 그대로 돈다(3주 / ▼1위 확인).

---

### 성과 라벨을 바꾼 이유 (S4)

| 기존 | 변경 |
| --- | --- |
| 소재 납품 및 캠페인 운영 대행 시 | 풀 프로젝트 수행 성과 (소재 제작 + 캠페인 운영) |
| 프로젝트 총괄 시 | 풀 프로젝트 수행 성과 (프로젝트 총괄) |
| 연간 공식 대행 시 | 연간 공식 프로젝트 수행 (바이럴 컨텐츠 기획·제작) |

기존 표현은 "이 패키지를 사면 이 성과가 난다"로 읽힐 여지가 있었다.
섹션 최상단에 출처 문장도 한 줄 올렸다 — 사례보다 **먼저** 읽혀야 한다.

### 로고월 파일 분리

`System` 섹션이 Service와 Clients 로고월 **사이**에 들어가야 해서,
한 컴포넌트에 묶여 있던 로고월을 `s-services.tsx` → `s-clients.tsx` 로 뺐다.
마크업·로고 목록·문구는 그대로. 섹션 순서는 `app/page.tsx` 한 곳에서만 조립한다.

### 모바일에서 1px 때문에 깨질 뻔한 것

히어로 숫자칩을 3등분(`grid-cols-3`)하면 390px 화면에서
`₩2,000만`의 실측 폭이 **90px**, 칸의 가용 폭이 **89px** 이었다. 딱 1px 모자라 줄이 깨진다.
좁은 화면에서만 마지막 칸에 폭을 더 준다 — `grid-cols-[1fr_1fr_1.4fr] sm:grid-cols-3`.
(눈으로 안 보이는 차이라 브라우저에서 폰트 실측으로 확인했다. 320px SE 기준까지 여유 확인.)

---

### 미반영 — S3 크루 인물 카드화

기획안은 역할 리스트를 **인물 사진 카드**로 바꾸라고 했다. 못 했다.
**팀원 인물 사진이 저장소에 한 장도 없다**(있는 건 촬영 현장 사진 8장뿐).
기획안 자체가 스톡·AI 생성 인물·실루엣 placeholder를 전부 금지했으므로
만들 수 있는 카드가 0장이다. 기존 역할 리스트를 그대로 뒀다.

→ **사진 + 각 멤버 공개 동의**만 확보되면 바로 붙는다. 기획안 반영 순서에서도 마지막 항목.
기획안 카드3의 경력 문구(`30여 브랜드 그로스 프로젝트 총괄`)만 `lib/crew.ts` GH 줄에 반영했다.

### 미확정 값 처리 (기획안 하단 체크리스트)

| # | 항목 | 처리 |
| --- | --- | --- |
| 1 | 연 거래액 3억대 | 기획안 문구 그대로 게시. **사실 확인 미완** — 다르면 `hero-stats.tsx` 한 줄만 수정 |
| 2 | 누적 제작 소재 편수 | 집계 근거 없어 **미노출**. 기획안 규칙("집계 불가 시 삭제") 적용. System 하단은 `브랜드 프로젝트 30+` 만 |
| 3 | 크루 인물 노출 범위 | 미확인 → S3 자체를 보류 |
| 4 | 클라이언트 성과 수치 | 기존 값 유지, NDA 재확인 대기 |

허위·과장 광고 리스크가 걸린 값은 **비워 두는 쪽**을 택했다.
근거 섹션에서 검증 안 된 숫자를 하나 얹는 순간 나머지 숫자도 같이 의심받는다.

---

## 보안 점검 (3일차)

| 항목 | 결과 |
| --- | --- |
| `.env*` git 추적 | `.env.example` 만 추적. 실제 키 파일 0건 |
| untracked 대용량 원본(1.6GB) | `git add -A` 실행했으나 `.gitignore` 로 **전량 차단 확인** — 커밋에 자산 0건 |
| 이번 커밋 내용물 | 소스 14개 파일 (+470/−199행). 이미지·영상 0건 |
| 서버 전용 키 참조 위치 | `lib/growth-ai.ts` · `lib/toss.ts` · `lib/supabase/server.ts` 3곳뿐. **`"use client"` 파일 참조 0건** (2일차와 동일) |
| 하드코딩 시크릿(`sk-ant-`·`sk_live`·JWT) | 0건 |
| 신규 파일의 client/server 경계 | `hero-stats`·`use-count-up`·`s-clients` = client(브라우저 API 사용) / `s-system`·`s-beyond` = server. 오용 없음 |
| API 라우트·인증·결제 로직 | **변경 0건** — 이번 작업은 랜딩 표시 계층만 |

## 과금 점검 (3일차)

| 항목 | 수치 | 판단 |
| --- | --- | --- |
| 신규 정적 자산 | **0건** | 텍스트 섹션 3개만 추가. `public/` 82MB 변동 없음 |
| 신규 코드 | 11KB (4파일) | 번들 영향 무시 가능 |
| 랜딩 HTML 전송량 | 33KB (gzip) | 섹션 2개 늘었지만 그대로 30KB대 |
| 신규 런타임 비용 | IntersectionObserver 1종 | 화면 밖 요소에만 붙고 **1회 발화 후 `disconnect`**. 상시 구독 없음 |
| 외부 API 호출 | 증가 0 | AI·결제·Supabase 호출 경로 손대지 않음 |
| 배포 횟수 | 프로덕션 2회 | (모바일 1px 수정으로 1회 추가) |

이번 작업으로 늘어난 반복 비용은 **없다**. 오히려 카운트업 수정으로
링크 미리보기·검색 노출에서 성과 숫자가 제대로 나가게 됐다.

---

## 다음 (4일차 예정)

- **S3 크루 인물화** — 팀원 사진 + 공개 동의 확보 후 (기획안 카드 6장 분량 준비돼 있음)
- 체크리스트 1번(연 거래액) 사실 확인 → 필요 시 `hero-stats.tsx` 수정
- 체크리스트 2번(누적 소재 편수) 집계 가능해지면 System 하단 한 줄에 추가
- 어드민 대시보드 (2일차 이월)

---

## 2026-08-08 ~ 08-10 — 4일차 · 상품 재설계 · 진단 · 가입 · 어드민 · 메일 · 도메인

3일차까지가 "보여주는 페이지"였다면 4일차는 **파는 구조와 파고 난 뒤의 구조**를 만들었다.
랜딩 한 장짜리 소개서에서, 신청 → 가입 → 어드민이 플랜을 걸고 → 클라이언트가
자기 진행을 보는 **한 바퀴가 도는 서비스**가 됐다. 서비스 이름도 이 시점에 확정했다.

### 서비스·법인 표기 확정

`해그로시 숏폼 스튜디오` / 운영 주체는 `주식회사 해그로시`. 표기를 `lib/constants.ts`
한 곳(`SERVICE`, `COMPANY`)으로 모으고 랜딩·약관·개인정보처리방침·메일이 전부 거기서 읽게 했다.

**해킹마케팅랩은 전 페이지에서 제거했다.** 휴업 중인 상호를 내가 임의로 푸터·약관에
넣어 뒀던 게 잘못이었다. 사업자번호 187-87-02820, 소재지 경기 김포시 김포한강10로 133번 127,
438 E180호 — 실제 법인 정보로 통일.

푸터는 이용약관 / 개인정보처리방침 / IMC 프로젝트(`hgrs.io/partnership`) / `hgrs.io` 만 남겼다.

### 가격 — 두 번 갈아엎었다

처음 잡은 표(1편 21만 / 5편 95만 / 10편 180만 / 20편 240만)는 **원가가 안 남는 표였다.**
편수가 늘수록 편당가가 급락하는 구조라, 20편을 팔면 팔수록 손해였다.

다시 잡은 게 지금 표다. 편당가 하락 폭을 완만하게 만들되, 묶음 할인이 **눈에 띄게
싸 보이지 않도록** 총액 기준으로만 이득이 보이게 했다.

| 구분 | 싱글 | 시딩 | 패키지 |
| --- | --- | --- | --- |
| 1편 | ₩210,000 | — | — |
| 스타터 | 5편 ₩990,000 | 10명 ₩550,000 | ₩1,540,000 |
| 그로스 | 10편 ₩1,890,000 | 20명 ₩990,000 | ₩2,880,000 |
| 스케일 | 20편 ₩3,490,000 | 30명 ₩1,380,000 | ₩4,870,000 |

명칭은 **스타터 / 그로스 / 스케일** 3종으로 통일하고, 시딩이 붙으면 "○○ 패키지".
싱글 3개와 패키지 3개는 **같이 사는 게 아니라 둘 중 하나를 고르는 것**이라
가로 3개씩 두 줄로 분리했다. 헤드 코칭 항목은 상품에서 완전히 뺐다.

### 가격을 랜딩에서 내렸다

만들어 놓고 보니 가격표가 첫 화면 허들이었다. **랜딩에서 가격 노출을 걷어내고**
(`s12-pricing.tsx`는 남겨 두되 `app/page.tsx`에서 마운트 해제) 신청 폼으로 받는다.

단, **진단 결과 안에서는 플랜명과 단가를 그대로 보여준다.** 자기 상황을 5문항 답하고
난 사람에게는 숫자가 허들이 아니라 답이다. 가격을 아예 감추면 진단이 광고가 된다.

### 컨텐츠 진단 (`lib/diagnosis.ts` · `s-diagnosis.tsx`)

신청 폼 앞에 5문항을 놓았다 — USP 이해도 / 목적 / 소스 보유 / 소재 재고 / 필요 물량.
문항마다 **왜 묻는지 한 줄**을 붙였다. 답을 조합해 플랜과 근거를 돌려준다.

- 소스가 없다 → 시딩으로 소스컷부터 확보 → 패키지
- 소스가 충분하다 → 숏폼 기획제작만 → 싱글
- 물량·목적으로 스타터/그로스/스케일 결정

진단 결과에서 **"헤드에게 문의" · "상담 신청" 류 CTA는 전부 뺐다.** 우리는 상담을 파는
회사가 아니다. AI가 쓴 티 나는 결과 헤드라인도 뺐다(`headline`은 코드에만 남고 화면에 안 나감).

진단에서 고른 값은 **아래 신청 폼에 그대로 체크된 채로 내려온다**(`diagnosis-context.tsx`).
같은 걸 두 번 묻지 않는다.

### 가입 — 이메일 3단계

소셜 로그인 없이 **이메일 + 비밀번호**. 우리 DB에 남기고 나중에 이메일 마케팅을 하려면
소셜로는 안 된다.

1. 이메일 입력 → 인증번호 발송
2. 인증번호 확인
3. 비밀번호 / 회사명 / 담당자 이름 / 직책 → 가입 완료 → 대시보드

동의는 2건. **개인정보 보호법 제15조**(수집·이용) / **정보통신망법 제50조**(광고성 정보 수신).
문구를 법령 명칭 그대로 쓰고 약관 전문과 법인 정보를 담았다(`lib/consents.ts`,
`CONSENT_VERSION = "2026-08-10"`). 동의 이력은 `user_consents`에 버전과 함께 적재.

**이메일 도메인 필터링은 없다.** 지메일·네이버 다 된다 — 실무 담당자는 회사 메일을 안 쓴다.

QA 편의를 위해 "이미 인증번호를 받았어요" 경로를 정식 기능으로 넣었다(세션 쿠키를
브라우저로 주입하는 우회는 하지 않았다).

### 클라이언트 대시보드 (`/app`)

좌측 = 계정(담당자·이메일·회사·직책) + 구매한 플랜 + 진행 시작일 + 결제 금액.
우측 = 진행중인 캠페인.

**단계 표기는 사장님이 부르는 말 그대로** 박았다(`lib/stages.ts`, DB check 제약과 1:1).

- 인플루언서 시딩 — 컨텐츠 가이드라인 작업중 / 모집중 / 확정 / 제품및서비스배송중 / 컨텐츠제작중 / 채널라이브확인 (싱글 플랜은 `해당없음`)
- 숏폼 기획제작 — 소스컷확인중 / 기획중 / 제작완료 / 최종수정반영중 / 다운로드하기

스텝퍼는 **큰 원 안에 1·2·3·4, 사이에 화살표**. 줄바꿈 없이 가로 스크롤(`stage-steps.tsx`).
기한은 **적용 시작 + 7일**로 D-7 뱃지가 뜬다.

패널 3종 — 가이드라인 8필드 작성 / 1차 선정 심사(후보 지표 보고 선택) /
산출물 미리보기 + 수정 요청(편당 무상 1회) + **최종본은 프로젝트 폴더 하나로 전체 다운로드**.

### 어드민 (`/admin`)

문의 목록 오른쪽에 [소개서 발송]과 플랜 선택 + **[적용 시작]**. 적용 시작을 누르면
프로젝트가 생성되고 시작 안내 메일이 나간다.

프로젝트 상세에서 단계 전환 / 인플루언서 후보 / 산출물 미리보기 임베드 / 드라이브 링크.
드라이브 링크는 **프로젝트당 하나**로 맨 위에 뒀다 — 편마다 따로 줄 일이 없다.

### 인플루언서 지표 자동 수집 (`lib/channel-metrics.ts`)

후보는 **채널 링크만 넣는다.** 팔로워·컨텐츠수·평균 조회/좋아요/댓글·프로필 이미지는
Apify 액터를 동기 실행해 받아온다.

| 플랫폼 | 액터 |
| --- | --- |
| 인스타그램 | `apify~instagram-profile-scraper` |
| 틱톡 | `clockworks~tiktok-scraper` |
| 유튜브 | `streamers~youtube-scraper` |

CPV는 벤더가 주지 않는다(우리가 협상해 넣는 단가 ÷ 평균 조회수) — 둘 다 있을 때만 계산한다.
**못 가져온 값은 절대 지어내지 않고 `null` → 화면에 `—`.** 클라이언트가 그 숫자를 보고
사람을 고르기 때문이다. 벤더 수명이 짧아 이 파일 하나만 갈아 끼우면 되게 격리했다.

실측: `aamoonlog` → 팔로워 2,541 / 게시물 56 / 평균 조회 8,249 / 좋아요 153 / 댓글 23, 약 5초.

### 메일 (Resend)

Supabase 내장 SMTP는 **시간당 2통 · 조직 멤버에게만** 발송된다. 가입 인증부터 막혔다.
Resend로 갈아탔다. 후이즈에 DKIM·SPF 레코드를 넣어 **Verified** 확인, 외부 주소로
실제 발송 성공(id `e788fe59-…`).

- 발신: `해그로시 숏폼 스튜디오 <studio@hgrs.io>` (`NOTIFY_FROM_EMAIL`)
- 회신: `ceo@h-grs.com`
- 발송 결과는 `email_log`에 `sent` / `failed` / **`skipped`**(키 미설정)로 남는다

### 도메인

`shortform.hgrs.io` CNAME → `cname.vercel-dns.com`. hgrs.io 루트는 Framer가 계속 쓰고
서브도메인만 Vercel로 뗐다. 코드·메일·메타데이터의 기준 URL을 전부 이 도메인으로 바꿨다.
`/admin`은 관리자, `/app`은 클라이언트.

### 랜딩 — 실제 대시보드를 그대로 보여준다

서비스 소개 미리보기 6종(`service-previews.tsx`)을 **실제 대시보드 화면과 같은 모양**으로
다시 그렸다. 1차 선정 심사 카드와 스텝퍼는 진짜 컴포넌트와 동일한 레이아웃이다.
파는 것과 보여주는 것이 다르면 안 된다.

그 외 반영: 크루 사진 8장 상단 모자이크로 이동(하단 마퀴에서 lazy 로딩돼 회색 박스로만
보이던 걸 전량 eager). 헤더 메뉴는 PC·모바일 모두 **컨텐츠 진단 / 로그인 / 내 프로젝트** 셋.
파트너십 외부 링크 제거 — 읽기도 전에 이탈시킬 이유가 없다.
"총괄·헤드·리드" 직함 표기 전면 삭제(수평 롤). 퍼포먼스 미디어바이어 이력 반영.
조나단 영상 포함 유튜브 롱폼을 숏폼과 같은 섹션 하단에 배치.

### 다시 밟은 함정 — CSS Grid `min-width: auto`

모바일 히어로가 잘리는 문제를 **두 번** 겪었다. 첫 시도는 텍스트 열에 `min-w-0`을
붙였는데 대상이 틀렸다. 진범은 `-mx-5 lg:hidden` 마퀴 래퍼로, 내부 폭이 3,208px라
그리드 트랙을 통째로 밀어내고 있었다. 래퍼에 `min-w-0 overflow-hidden`.

검증은 **390px iframe**으로 했다(이 환경에선 창 리사이즈가 안 먹는다).
`documentElement.scrollWidth − innerWidth = 0` 확인.

> 그리드 자식은 기본이 `min-width: auto`라 **내용보다 작아지지 않는다.**
> 가로로 흐르는 요소를 넣을 땐 그 요소의 래퍼에 `min-w-0`을 붙인다. 세 번째는 없어야 한다.

### 그 외 고친 것들

| 증상 | 원인 | 조치 |
| --- | --- | --- |
| `/admin` 들어가도 클라이언트 화면 | `requireProfile`이 `next` 없이 `/login`으로 보내 로그인 후 항상 `/app` | `?next=` 전달 + admin 자동 라우팅 |
| 빌드 실패 | `"use server"` 파일에서 상수 객체 export | INITIAL state를 클라이언트 컴포넌트로 이동 |
| 프리렌더 오류 | `useSearchParams` | 서버 page + `<Suspense>` + 클라이언트 폼으로 분리 |
| TS `never` | `.update({[column]: stage})` 동적 키 | 명시적 삼항 패치 객체 |
| eslint effect 경고 | `s-apply.tsx`에서 useEffect로 상태 동기화 | 렌더 중 상태 조정 패턴 |
| 최종 다운로드 버튼 2개 | `s.replace`가 CandidatePanel을 먼저 매칭 | **4일차 마감 감사에서 발견·제거** |
| `source .env.local` 파싱 오류 | `NOTIFY_FROM_EMAIL` 값의 `<>`를 쉘이 리다이렉트로 해석 | 값 따옴표 처리 (**마감 감사에서 발견**) |

## 보안 점검 (4일차)

| 항목 | 결과 |
| --- | --- |
| `.env*` git 추적 | `.env.example` 만 추적. 실제 키 파일 0건 (3일차와 동일) |
| 하드코딩 시크릿 | 0건 — `service_role` 문자열 매치는 전부 주석 |
| 서버 전용 키 참조 위치 | `app/admin/actions.ts` · `lib/toss.ts` · `lib/mail.ts` · `lib/growth-ai.ts` · `lib/supabase/server.ts` · `lib/channel-metrics.ts` 6곳. **`"use client"` 파일 참조 0건** |
| RLS | public 15개 테이블 **전부 ON**, 정책 미보유 테이블 0건 |
| admin 클라이언트 사용처 | `/app` 조회는 조인 편의로 admin 클라이언트를 쓰되 **`project.user_id`로 직접 필터** — 남의 프로젝트 id가 섞일 경로 없음 |
| 클라이언트 액션 권한 | `saveGuideline`·`toggleCandidate`·`requestRevision` 전부 `ownProject` 검증 통과 |
| 동의 이력 | `user_consents`에 버전(`2026-08-10`)과 함께 적재 — 법령 대응 가능 |
| **관리자 비밀번호** | `ceo@h-grs.com` 이 **개발용 약한 비밀번호** 상태. 실판매 전 교체 필요 ⚠️ |
| 테스트 계정 잔존 | `dev-test@…local` · `dev-test2@…local` · `qa-admin@hgrs.io` 3건. 실판매 전 정리 권장 |
| Resend Enable Receiving | **켜져 있음** — 루트 MX가 hgrs.io 수신을 가로챌 수 있어 꺼야 함 ⚠️ |

## 과금 점검 (4일차)

| 항목 | 수치 | 판단 |
| --- | --- | --- |
| Apify | FREE 플랜 · 월 $5 크레딧 · **이번 달 사용 $0** | 프로필 1건당 약 $0.0023. 월 2,000건까지 무료 안. 후보 등록 시에만 호출되므로 트래픽과 무관 |
| Resend | 무료 3,000통/월 | 실제 발송 극소량. `email_log` 현재 `skipped` 2건(키 설정 전 시도) |
| Vercel 프로덕션 배포 | 이번 세션 20회 | 빌드 시간만 소모. 캐시 복원 중이라 회당 27초 |
| `public/` 정적 자산 | **84MB** (clips 49MB + reels 21MB) | **유일한 실질 대역폭 원가.** 트래픽이 붙으면 여기서 청구된다 |
| 미참조 가능 영상 | 약 17MB (7개 추정) | 슬러그를 템플릿으로 만들어 정적 판별이 불확실 — **삭제하지 않았다.** 실측 후 정리 |
| Supabase | Free tier 내 | 테이블 15개, 행 수 미미 |
| 신규 반복 비용 | Apify 호출뿐 | 관리자가 후보를 넣을 때만 발생. 사용자 트래픽에 비례하지 않음 |

인플루언서 검색을 **공개 서비스로 여는 안은 접었다.** 비용이 트래픽에 비례해 늘고,
핵심 가치를 희석하며, 이미 포화된 시장이다. 나중에 게이트 걸린 리드 마그넷 형태면 몰라도.

## 다음 (5일차)

- **첫 실거래 1건** — 지금 구조로 한 바퀴 실제로 돌려보는 게 최우선
- 관리자 비밀번호 교체 + 테스트 계정 3건 정리 (실판매 전 필수)
- Resend "Enable Receiving" 끄기
- **블로그** — 이 앱의 `/blog` 경로에 붙인다(`blog.hgrs.io` 아님). 서브디렉토리가 도메인 권위를 나눠 갖지 않는다.
  5필러 × 5유형 편성표 확정 → **1편 먼저 생성해 톤 고정** → 그 다음 자동화 인프라
- hgrs.io 통합 (Framer 이탈, `/coaching` → 브랜드-AI 개편, `/partnership` 구독제) — 2~3주 뒤
- `public/` 미참조 영상 실측 후 정리

---

## 2026-08-11 — 5일차 · 소개서(PDF) 제작과 발송 파이프라인

4일차가 "한 바퀴 도는 서비스"였다면 5일차는 **파는 문서**를 만들었다.
플랜 문의가 들어오면 나가는 소개서 한 부와, 그것을 실어 보내는 메일이다.

### 소개서는 홈페이지 복사본이 아니다

처음엔 홈페이지 내용을 A4 세로로 옮겨 15페이지를 만들었다. 그건 소개서가 아니라
**문서화된 홈페이지**였다. 사장님 지적으로 세 번 갈아엎었다.

| 회차 | 무엇이 틀렸나 | 어떻게 고쳤나 |
| --- | --- | --- |
| 1차 | 홈페이지를 옮겨 적기만 함 | 시장 구조·포지션·통합 라인 도식을 세워 **제안서**로 |
| 2차 | 글줄이 흐르는 장표, 워드 문서 같음 | 16:9 슬라이드로 전환, 도형·숫자·실제 화면으로 |
| 3차 | 제목을 전부 존댓말 문장으로 달아 어느 장인지 안 보임 | **키워드를 헤드라인(26pt)**, 제안 문장을 서브라인(15pt)으로 |

최종 17장.
표지 → 법인 소개 → 시장 구조 → 포지션 → 진행 라인 → 기대 효과 → 제작 조직 →
역할 구성 → 제작 시스템 → 클라이언트 히스토리 → 주요 성과 → 최근 주요 포트폴리오 →
진행 프로세스 → 내 프로젝트 → **플랜 안내** → 계약·결제 → 문의처.

### 조판에서 배운 것

- **키워드가 먼저 크게 보여야 한다.** 키워드를 작은 라벨로 내렸더니 제안 문장을
  읽어 주지도 않았다. 순서를 뒤집으니 바로 읽혔다
- **서브라인은 핵심 한 줄이다.** 더미텍스트 크기(9.5pt)로 줄여 뒀더니 안 읽혔다 → 15pt
- **색은 글자가 아니라 면에 쓴다.** 카피에 색을 입히는 것보다 배경·도형에 인디고와
  골드탄을 채우는 쪽이 훨씬 강했다
- **3인칭 소개투 금지.** "이런 분들이 만듭니다"는 남을 소개하는 말이다.
  우리가 우리를 소개하는 문서다
- **플랜은 중간에 두면 못 찾는다.** 뒷단(내 프로젝트 다음, 계약 앞)으로 옮겼다

### 실제 화면을 넣었다

목업이 아니라 라이브에서 캡처했다 — 내 프로젝트 대시보드, 1차 선정 심사(팔로워·평균
조회·CPV), 브랜드 AI 기본 분석. `public/deck/` 에 파일로 둔다.
성과 사례 4건에도 실제 소재 이미지를 넣었다(뤼이드·파크론·모에브·크래프톤).

### 값은 코드에서 읽는다

가격·단계·정책 문구를 소개서에 **하드코딩하지 않았다.** `lib/constants.ts` 와
`lib/stages.ts` 에서 읽는다. 하드코딩해 두면 사이트만 고치고 소개서는 옛 가격을
들고 돌아다니게 된다.

```
npm run deck          # HTML 생성 → 로컬 서버 → Chrome 헤드리스로 16:9 인쇄
소개서.html#pg7       # 그 장만 띄워 조판 확인 (:target CSS)
```

### 발송 메일

본문은 대표 디렉터 문안 그대로 싣는다. 촬영 현장 사진 한 장과
**1분 숏폼 소개서** 블록(첨부 + 링크)을 붙였다.

발신·회신을 **contact 로 통일**했다. 다만 `h-grs.com` 은 Resend 미인증이라
발신은 인증 도메인 `contact@hgrs.io`, 회신은 `contact@h-grs.com` 이다.
h-grs.com 을 Resend에 인증하면 발신 주소도 그대로 옮길 수 있다.

첨부는 Resend `path` 방식이다 — 10MB를 매 발송마다 base64로 싣지 않는다.

### 그 외 고친 것들

| 증상 | 원인 | 조치 |
| --- | --- | --- |
| 소개서 PDF가 배포에서 404 | `.vercelignore` 의 `*.pdf` | `!public/*.pdf` 예외 추가 |
| 첨부 422 invalid_attachment | 로컬 `NEXT_PUBLIC_SITE_URL` 이 localhost | 메일이 거는 주소를 공개 도메인으로 고정 |
| 시장 구조 도식에서 오른쪽 원이 잘림 | 원 지름이 그리드 열 폭보다 큼 | 열 폭 안으로(58mm) |
| 결제 조건 오기 | 카드 기본으로 적어 둠 | **세금계산서 발행 후 현금이 기본, 카드도 가능** 으로 정정 |

## 보안 점검 (5일차)

| 항목 | 결과 |
| --- | --- |
| `.env*` git 추적 | `.env.example` 만 |
| 하드코딩 시크릿 | 0건 |
| `"use client"` 파일의 서버 키 참조 | 0건 |
| RLS | public 15개 테이블 전부 ON |
| 신규 공개 자산 | 소개서 PDF·촬영 사진·UI 캡처 — **가격이 공개 URL에 올라간다.** 링크를 아는 사람만 열지만 검색 색인은 막지 않았다 ⚠️ |
| 메일 회신 주소 | `ceo@` 참조를 코드에서 전량 제거, `contact@h-grs.com` 으로 단일화 |
| **관리자 비밀번호** | 여전히 개발용 약한 값 — 실판매 전 교체 ⚠️ |
| 테스트 계정 | dev-test 2건 · qa-admin 1건 잔존 ⚠️ |
| Resend Enable Receiving | 켜져 있음 — 끄는 것을 권장 ⚠️ |

## 과금 점검 (5일차)

| 항목 | 수치 | 판단 |
| --- | --- | --- |
| Apify | FREE · 이번 달 **$0** | 후보 등록 시에만 호출 |
| Resend | 무료 3,000통/월 · 실제 발송은 테스트 6건 | 첨부를 링크(path)로 붙여 페이로드 부담 없음 |
| `public/` | 84MB → **96MB** | 소개서 PDF 11MB + 메일 이미지·UI 캡처 0.4MB 증가 |
| 소개서 PDF | 11MB | 다운로드 1회당 11MB 대역폭. 메일 첨부도 Resend가 같은 URL을 받아 간다 |
| Vercel 배포 | 이번 세션 12회 | 빌드 시간만 소모 |

소개서를 뿌리기 시작하면 **PDF 대역폭이 처음으로 의미 있는 원가**가 된다.
지금 11MB는 이미지 원본을 그대로 인쇄한 결과라, 배포량이 늘면 이미지 다운스케일로
절반 이하로 줄일 수 있다.

## 다음 (6일차)

- **첫 실거래 1건** — 소개서까지 갖췄으니 이제 실제로 한 바퀴
- 실판매 전 필수: 관리자 비밀번호 교체 / 테스트 계정 3건 정리 / Resend 수신 끄기
- `h-grs.com` Resend 도메인 인증 → 발신 주소도 contact@h-grs.com 으로
- 소개서 PDF 용량 최적화(이미지 다운스케일)
- 블로그 — 이 앱의 `/blog`, 편성표 확정 후 1편 먼저

---

## 2026-08-11 ~ 08-12 — 6일차 · 루트 도메인 이관 · /sns-brand · 모바일 · 블로그(철회)

프로젝트 성격이 이 날부터 바뀐다. `shortform.hgrs.io` 한 페이지짜리 서비스가
아니라 **hgrs.io 전체**를 이 앱이 서빙한다. 프레이머는 내렸다.

### 만든 것

| 영역 | 상태 |
| --- | --- |
| `hgrs.io` 루트 도메인 이관 (프레이머 → Vercel) | 완료 |
| `/` 홈 — 프레이머 원본 문안 그대로 이식 | 완료 |
| `/sns-brand` — 브랜드 SNS 채널 리드 확보 랜딩 | 완료 |
| `/portfolio` — 성과 사례 | 완료 |
| 문의 접수 시 소개서 자동 발송 (Resend) | 완료 |
| 소개서(PDF) 3개 서비스 라인 구조로 재편 — 30장 | 완료 |
| 모바일 웹 전면 수정 | 완료 |
| `/blog` 자체 블로그 | **철회 — 브런치북으로 리다이렉트** |

### 경로 구조 (확정)

```
hgrs.io/                 홈
hgrs.io/shortform        숏폼 스튜디오
hgrs.io/sns-brand        브랜드 SNS 채널 커뮤니케이션
hgrs.io/portfolio        성과 사례
hgrs.io/blog             → brunch.co.kr/brunchbook/bmaha2 (307)
```

레거시 경로는 전부 살렸다. `/partnership`·`/coaching` → `/sns-brand`,
`/portfolio/:slug` → `/portfolio`, `/contact` → `/sns-brand#contact`,
`shortform.hgrs.io/*` → `hgrs.io/*`.

### 판단 근거가 필요한 결정들 (6일차)

**`www` 리다이렉트를 걸었다가 뺐다.** apex 인증서가 발급되기 전 Vercel 이
apex → www 로 308 을 내보냈고, 그게 브라우저에 **영구 캐시**된 상태에서 반대
방향 308 을 얹으니 `ERR_TOO_MANY_REDIRECTS` 로 물렸다. 308 은 브라우저가
지우지 않는다. 지금은 www 도 200 으로 서빙하고 중복 색인은 canonical 이 막는다.
→ **교훈: 리다이렉트는 되돌릴 수 있는 방향으로 먼저 건다.** 블로그 리다이렉트를
`permanent: false` 로 둔 이유가 이것이다.

**CSS 스크롤 구동 애니메이션은 이 환경에서 죽어 있다.** `CSS.supports
('animation-timeline','view()')` 가 true 를 돌려주는데도 `timeline.currentTime`
이 null 이라 애니메이션이 끝 상태로 고정된다. `overflow:hidden` 조상 하나로도
비활성화된다. 검증 못 하는 걸 페이지 핵심 연출로 두지 않는다 →
`use-view-progress.ts` 로 JS 가 직접 잰다. 초기값은 항상 1(완성 상태)이라
JS 가 안 돌면 그냥 완성된 그림이 보인다.

**소개서는 랜딩 데이터를 직접 읽는다.** 처음엔 소개서용 문안을 새로 썼는데,
"홈페이지에 내용 다 있는데 왜 또 다른 걸 넣냐"는 지적을 받았다. 지금
`scripts/build-deck.ts` 는 `lib/sns-brand.ts` 를 import 한다. 랜딩을 고치면
소개서가 따라 바뀐다. 예외는 `DECK_HERO` 하나 — 트러스티는 랜딩에선 제품 컷,
소개서에선 수의사 컷을 쓴다(랜딩은 아래에 영상 카드가 붙어 겹치기 때문).

**문자열 치환으로 코드를 고치고 성공했다고 보고한 적이 있다.** prettier 가
포맷을 바꾼 뒤라 python `.replace()` 대상이 안 맞았는데, 실패가 조용히 지나가
"고쳤다"고 말했다. 같은 지적을 세 번 받았다. → **블록은 통째로 다시 쓰고,
고쳤다고 말하기 전에 실제 결과물을 본다.** 이 세션에서 만든 검사 스크립트 셋이
그 대응이다.

### 모바일 웹 (사장님 지적 3건)

| 지적 | 원인 | 조치 |
| --- | --- | --- |
| 상단에 로그인·내 프로젝트가 없다 | `cta` 를 넘긴 페이지에서 `HeaderAuth` 대신 CTA 만 렌더 | 인증 버튼 상시 노출로 고정 |
| 카테고리를 볼 수 없다 | nav 가 `hidden lg:flex` — 모바일엔 메뉴 자체가 없음 | 햄버거 패널 (`components/site-nav.tsx`) |
| 이미지·영상이 중복되고 잘린다 | 아래 표 | 아래 표 |

**중복** — 같은 컷이 한 페이지에 두 번 나오던 곳.

- 트러스티 히어로(`trusty-vet.jpg`)가 바로 아래 영상 `LCQywT3a2pQ` 의 썸네일과
  같은 프레임 → 히어로를 제품 컷으로
- PROCESS 4장이 **전부** 위 사례 섹션에서 이미 쓴 이미지 → 미사용 자산
  (`krafton-3c`·`lovedy-contents`·`trusty-vet`)으로 교체
- 포트폴리오 월의 3편이 사례 카드에서 이미 튼 영상 → 제거 (17 → 14편)
- 원칙: **한 페이지 안에서 이미지는 한 번씩만 쓴다**

**잘림**

- 사례·프로세스 도판 `object-cover` → `object-contain`. 기획안·퍼널 설계처럼
  글자가 박힌 도면이 4:3 틀에 잘려 오른쪽 라벨이 통째로 날아갔다
- Capability 가로 트랙은 `lg` 이상만. 폰에서는 카드 한 장(라벨 옆에 붙는 첫
  카드 460px)이 화면보다 넓어 **가로 트랙을 유지하는 한 무슨 수를 써도 잘린다**
  → 세로로 쌓는다. 모든 카드 폭을 19rem 이하로 맞췄다(360px 화면 기준)
- BrandOrbit 수렴 연출도 `lg` 이상만 (`useViewProgress({minWidth})`). 출발
  위치가 화면 밖(`fx: 224%`)이라 폰에서는 중간 상태가 "위성이 오른쪽에 겹쳐
  글자가 포개진 그림"으로 멈춰 보인다 — 연출이 아니라 고장으로 읽힌다
- 포트폴리오 월 모바일 2열 + 자동재생 iframe 해제. 로딩 중 iframe 아래로
  썸네일이 비쳐 같은 컷이 겹쳐 보였고, 셀룰러에서 14편이 도는 것도 곤란하다

### 블로그 — 만들었다가 접었다

2편을 썼고 발행까지 했다가 **같은 날 철회했다.** 사장님 평가: "이미지도 글도
본문도 제목도 구성도 다 형편없다."

남긴 판단: 블로그는 한 편씩 손으로 쓸 게 아니라 **기획-제작-발행 자동화를
따로 설계할 대상**이다. 그때까지 `/blog` 는 이미 글이 쌓여 있는 브런치북으로
보낸다. `scripts/blog-image.mjs`(gpt-image-1 도판 생성)는 남겨 뒀다 —
자동화 설계할 때 다시 쓸 도구다.

> 도판 프롬프트에 한글을 넣지 않는다. 모델이 그리는 한글은 깨진다.
> 글자는 캡션으로 붙인다. 배경색은 "flat solid, no gradient, no glow" 까지
> 못 박아야 한다 — 안 그러면 금색 그라데이션 같은 게 나온다.

### 만든 검사 도구 3종

눈으로 왕복하지 않으려고 만들었다. 전부 의존성 없이 Chrome 헤드리스 + CDP.

```
node scripts/mobile-audit.mjs  [url] [width]   # 화면 밖으로 나간 요소를 DOM에서 지목
node scripts/mobile-shots.mjs  <path> <out>    # 390px 폭으로 한 화면씩 캡처
node scripts/secret-scan.mjs                   # 빌드 결과물에 서버 키가 박혔는지
```

`mobile-audit` 은 `overflow: hidden/auto/scroll` 조상 아래는 봐준다 —
마퀴처럼 일부러 넘치게 만든 것까지 잡으면 진짜 문제가 묻힌다.

## 보안 점검 (6일차)

| 항목 | 결과 |
| --- | --- |
| `.env*` git 추적 | `.env.example` 만 · 히스토리에도 없음 |
| **빌드 결과물에 서버 키 유출** | **0건** — 정적 청크 41개 + 프리렌더 HTML 전수 검사 (`scripts/secret-scan.mjs`) |
| RLS | public 15개 테이블 전부 ON |
| `/admin` | `requireAdmin()` 서버 판단 + `robots: noindex` |
| `/api/growth-ai` | `requireProfile()` — 로그인 필수 |
| `/api/payments/confirm` | 승인 금액은 **DB 값**으로 보냄, 쿼리 amount 는 대조용. 불일치 시 차단, `paid` 재승인 없음 |
| `/api/payments/webhook` | 본문을 신뢰하지 않음 — orderId 만 꺼내 토스 조회 API 로 재확인 |
| 소개서 PDF 공개 URL | 가격이 열려 있음 (링크 아는 사람만, 색인은 안 막음) ⚠️ |
| **관리자 비밀번호** | 여전히 개발용 약한 값 ⚠️ |
| 테스트 계정 | dev-test 2건 · qa-admin 1건 잔존 ⚠️ |
| Resend Enable Receiving | 켜져 있음 ⚠️ |

**남은 것 하나**: `/api/payments/webhook` 은 인증이 없으므로 임의 orderId 로
호출하면 그때마다 토스 조회 API 를 부른다. 금전 피해는 없지만(본문을 안 믿으니)
**호출량은 남이 만들 수 있다.** 실거래가 돌기 시작하면 IP 레이트리밋을 건다.

## 과금 점검 (6일차)

| 항목 | 수치 | 판단 |
| --- | --- | --- |
| Vercel 배포 | 이번 세션 20회 | 빌드 시간만 |
| `public/` | 96MB → **108MB** | 로고 17종 + `/sns` 자산 20종 + 홈 히어로 |
| `.git` | 295MB | 이미지·PDF·mp4 가 커밋돼 있다. 클론이 무거워진다 |
| OpenAI | 도판 3장 (gpt-image-1, high) | 블로그 철회로 **프로덕션에서는 안 쓴다.** 키는 Vercel 에 남아 있음 |
| Anthropic | 온보딩 브랜드 분석에서만 · `max_tokens: 16000` | 로그인 사용자만 호출 가능하나 **호출 상한이 없다** ⚠️ |
| Apify | 어드민 액션에서만 | 안전 |
| Resend | 무료 3,000통/월 · 실발송 테스트 수 건 | 여유 |
| YouTube iframe | 데스크톱만 자동재생 (모바일 해제) | 대역폭은 유튜브 부담 |

**대시보드 확인이 필요한 항목** (CLI·MCP 로 조회 불가):
Vercel 대역폭·이미지 최적화 사용량, Supabase 무료 한도, 토스 정산 수수료.

## 다음 (7일차)

- **첫 실거래 1건**
- 실판매 전 필수: 관리자 비밀번호 교체 / 테스트 계정 3건 정리 / Resend 수신 끄기
- 블로그 자동화 설계 — 기획(편성표·키워드 전략) → 제작(원고·도판) → 발행.
  이번처럼 손으로 한 편씩 쓰는 방식은 품질이 안 나온다
- Search Console 등록 + 사이트맵 제출 (사장님 구글 계정 필요)
- `h-grs.com` Resend 도메인 인증 (Squarespace 접근 회복 후)
- `/api/payments/webhook` 레이트리밋
- `public/` 대용량 자산 다운스케일 — 소개서 PDF 11MB, `.git` 295MB

---

## 2026-08-12 — 7일차 · 블로그 재구축(벤치마크 → 규격 → 1편)

6일차에 만들었다가 같은 날 접은 `/blog` 를 **규격 기반으로 다시 지었다.**
사장님 요청은 "hgrs.io/admin 에 관리자 전용 어드민을 붙이고, 벤치마크 3곳의
퀄리티·편성·키워드를 따라 이미지와 본문 품질을 동일하게 맞추고 싶다" 였다.

**어드민을 먼저 짓지 않았다.** 한 편이 스펙에 맞는지부터 봐야 어드민이 의미가 있고,
톤이 틀리면 UI 까지 다시 만들어야 하기 때문이다. 그래서 이번 세션은
엔진 · 규격 · 검사식 · 1편까지만 하고 멈췄다.

### 벤치마크 3곳 — 성격이 정반대였다

브라우저로 목록과 실제 글 2편 내부까지 열어 확인했다.

| 대상 | 정체 | 우리에게 |
| --- | --- | --- |
| blog.daehong.com (대홍기획) | 인하우스 브랜드 저널리즘. DIGGING/WORK/INSIDE, 코너 브랜딩([월간 AㅏㅇI들]) | 검색 유입 설계 없음. **골격으로 베끼면 안 됨** |
| magazine.cheil.com (제일기획) | 인하우스 브랜드 저널리즘. 캠페인 케이스 서사 제목 | 같음 |
| **batcrew.co.kr (비에이티)** | **대행사 SEO 획득형** | **골격 대상은 여기 한 곳** |

비에이티 글 URL 은 `batcrew.co.kr/blog-insight-{영문키워드}/` (목록만 `/blog/insight/`).

**해부해서 나온 정형:** 읽는 시간 명시 → 정의형 한 문장 리드 → 목차 →
**질문형 H2 6~8** → **비교표 2~4** → FAQ 4문항 → 맺으며 + 다음 편 예고 →
CTA 2개 → 관련 아티클 3. 8,500자/19분, 내부링크 5.

### ⚠️ 이미지 문제의 답이 벤치마크 안에 있었다

비에이티는 **8,500자 글에 이미지가 썸네일 1장뿐**이고, 시각 요소의 주력이 **표**다.
6일차 `/blog` 가 무너진 큰 이유가 AI 도판을 억지로 채운 것이었으므로,
**본문 도판을 규격으로 금지하고 표로 대체**했다. 글자 깨짐·비용·품질 문제가
한 번에 사라진다. gpt-image-1 은 썸네일 1장에만 남긴다.

### 사장님 결정 (2026-08-12)

- 본문 골격 = **BAT형 실무 가이드 하나로 고정.** 매거진 서사는 쓰지 않는다.
- 편성 필러 5개 = 숏폼·전환소재 / 브랜드SNS운영 / 크리에이터·시딩·커머스 /
  AI마케팅자동화 / 브랜드스케일업(헤드텀).

### 만든 것

| 파일 | 역할 |
| --- | --- |
| `lib/blog-spec.ts` | 편성표(5필러 × 4유형) + 발행 규격 |
| `lib/blog-ai.ts` | planPost(구성안 JSON) → writePost(본문). claude-opus-5, 본문은 스트리밍 |
| `scripts/blog-write.ts` | 1편 생성기. `--from-plan` 으로 구성안 재사용 가능 |
| `scripts/blog-audit.ts` | 규격 미달이면 발행 차단 |
| `supabase/migrations/20260812000001_blog.sql` | blog_keyword / blog_post. **아직 미적용** |

**규격 숫자는 `lib/blog-spec.ts` 한 곳에만 있다.** 그 값이 생성 프롬프트와
발행 검사식 양쪽에 들어간다. 두 벌로 두면 프롬프트만 고치고 검사식은 옛 기준으로
남는 사고가 난다. 필러·유형을 DB 테이블로 빼지 않은 이유도 같다.

### 검사식이 실제로 막은 것 (3회 재생성)

| 회차 | 걸린 것 | 눈으로 봤다면 |
| --- | --- | --- |
| 1차 | `<h2 id="1">` HTML 헤딩, 내부링크 0, FAQ 형식 | 목차·구조화 데이터가 죽은 채 발행 |
| 2차 | 내부링크 0 — 실은 `http://localhost:3000/shortform` 이 본문에 박힘 | **링크 전부 죽은 채 발행** |
| 3차 | 없음 — 통과 | — |

2차가 핵심이다. `SERVICE.url` 이 `NEXT_PUBLIC_SITE_URL` 을 따라가는데 스크립트가
`.env.local` 을 읽어 로컬 주소가 원고에 그대로 들어갔다. **콘텐츠가 실행 환경을
타면 안 된다.** 두 가지로 막았다 — 본문 내부 링크는 **상대 경로**(`/shortform`)로
고정, `CANONICAL_URL` 가드로 브랜드 컨텍스트는 항상 `https://hgrs.io`.

모델이 목차 앵커를 만들려고 HTML 헤딩을 쓰는 것도 재현성 있는 함정이라
프롬프트에 HTML 금지를 명시하고, 검사식에 HTML 헤딩 감지를 넣었다.
(이게 없으면 구조 검사가 전부 0으로 나와 원인을 오해한다.)

### 1편 결과

`drafts/brand-shortform-marketing-guide-2026.md`
**7,113자 · 질문형 H2 10 · 비교표 10 · FAQ 4 · 내부링크 6 · 본문 이미지 0** — 규격 통과.

제목: "브랜드 숏폼 마케팅이란? 조회수가 아니라 전환을 만드는 2026 완벽 가이드"

지어낸 수치가 한 건도 없다. 업계 평균·배수 표현 없이 구조와 판단 기준으로만
설명한다(검사식의 수치 경고 0건). 자사 성과 자랑도 없고 `/portfolio` 링크로만 보낸다.

검수용 프리뷰를 아티팩트로 발행해 사장님께 링크 전달 — **톤 승인 대기 중.**

## 보안 점검 (7일차)

| 항목 | 결과 |
| --- | --- |
| 새 파일 5개 시크릿 하드코딩 | **없음** (grep: sk-ant / service_role / password / token) |
| 원고에 내부정보·키 | **없음** |
| `.env.local` | `.gitignore` 34행에 포함, 추적 안 됨 |
| 프로덕션 영향 | **없음** — 배포 안 했고 앱 코드 변경 없음. `/blog` 는 여전히 브런치 307 |
| 어드민 노출면 | **아직 안 지음.** 지을 때 기존 `/admin` 의 `requireAdmin()`(role=admin 서버 판단, noindex) 아래에 붙이면 상속됨 |
| 새 RLS | blog_keyword = 어드민만 / blog_post = 발행분만 공개 + 초안은 어드민. `is_admin()` 을 `select` 로 감싸 행마다 재실행되지 않게 함. **단 마이그레이션 미적용이라 아직 검증 안 됨** |
| `drafts/` | `.gitignore` 에 없다 — 커밋하면 원고가 리포에 들어간다. 의도한 것인지 판단 필요 ⚠️ |

## 과금 점검 (7일차)

**자동으로 도는 것이 없다** — 이게 가장 중요하다.

| 확인 | 결과 |
| --- | --- |
| 앱에서 `blog-ai` 호출하는 경로 | **없음.** 스크립트 수동 실행만 |
| cron 설정 (`vercel.json` / `next.config.ts`) | **없음** |
| → 방치했을 때 발생하는 과금 | **0** |

**이번 세션 실사용 (실측):**

| 항목 | 실측값 |
| --- | --- |
| 원고 1편 출력 토큰 | **11,079** (`messages.countTokens` 실측) |
| 모델 단가 | claude-opus-5 · 입력 $5 / 출력 $25 per MTok |
| 호출 횟수 | 기획 1회 + 본문 3회 (형식 오류로 2회 재생성) |

출력 3편 합계는 대략 3.5만 토큰 → 약 $0.9. 여기에 **adaptive thinking
(effort: high) 토큰이 추가로 과금되는데 그 양은 응답에서 알 수 없다.**
그래서 정확한 금액은 추정하지 않는다 — **Anthropic 콘솔에서 확인할 것.**

**어드민을 붙일 때 반드시 넣어야 할 가드** ⚠️
지금은 사람이 터미널에서 돌리므로 상한이 필요 없지만, 어드민에 "생성" 버튼이
붙는 순간 클릭 한 번이 1편($1 안팎)이다. 연타·중복 생성 방지와 일일 생성 상한을
UI 가 아니라 서버에서 걸어야 한다. 6일차에 지적된 "Anthropic 호출 상한 없음"
문제와 같은 종류다.

## 다음 (8일차)

1. **사장님 톤 검수** — 통과해야 나머지가 의미 있다. 분량(7,113 vs 벤치마크 8,500),
   표 개수(10개는 섹션마다 하나라 다소 촘촘. 벤치마크는 2~4개), 문체.
   셋 다 `lib/blog-spec.ts` 숫자 하나로 생성·검사 기준이 함께 움직인다
2. `/admin/blog` 3정거장 — 편성 / 키워드 보드 / 기획소→제작소. **생성 상한 가드 포함**
3. 마이그레이션 적용 → 키워드 보드에 5필러 헤드텀 등록
4. `/blog/[slug]` 발행면 + `next.config.ts` 브런치 리다이렉트 해제 + sitemap 등록
5. `--prod` 배포 후 **라이브 URL 로 재확인** (이 레포는 프리뷰 환경변수가 없다)
6. `drafts/` 를 커밋할지 `.gitignore` 할지 결정

6일차에서 넘어온 것(첫 실거래, 관리자 비밀번호 교체, 테스트 계정 정리,
Resend 수신 끄기, Search Console 등록)은 그대로 남아 있다.

---

## 2026-08-13 — 8일차 · 작업자 대시보드 (별도 호스트)

영상 기획·제작을 맡길 외부 작업자용 표면을 만들었다. **레포도 DB도 하나다. 호스트만 다르다.**

### 왜 복사하지 않았나

작업자 화면과 클라이언트 화면은 같은 `deliverables` 행을 본다. 작업자가 결과물을 올리면
클라이언트 대시보드가 그 자리에서 바뀌어야 하고, 클라이언트가 누른 수정 요청이 작업자
카드에 그대로 떠야 한다. 레포를 복사하면 그 연결이 끊기고 두 벌을 평생 같이 고쳐야 한다.

### 가르는 지점은 네 군데다

| 층 | 조치 |
| --- | --- |
| 호스트 | `WORK_HOSTS` 에 걸린 도메인이면 `/work/**` 외 **전부 404** (`proxy.ts`). 리다이렉트가 아니라 404 — 리다이렉트는 "다른 곳이 있다"를 알려준다 |
| 페이지 소스 | 루트 레이아웃에서 JSON-LD(Organization/WebSite)와 채널톡을 걷어내 `app/(site)/layout.tsx` 로 내렸다. 기존 라우트 10개를 `(site)` 그룹으로 옮겼다(URL 불변) |
| 데이터 | 작업자 쿼리는 `profiles` / `plans` / `orders` / `inquiries` 를 **한 번도 조인하지 않는다.** 프로젝트 이름은 `projects.work_code`(W-0001) 하나뿐 |
| 브리프 | 클라 가이드라인 원문 대신 상호·브랜드명·URL을 걷어낸 `work_briefs` 파생본만 |

### 브라우저에 Supabase 클라이언트를 올리지 않는다

작업자 표면은 로그인까지 전부 서버 액션이다. `createBrowserClient` 를 한 번이라도 쓰면
`NEXT_PUBLIC_SUPABASE_ANON_KEY` 가 그 번들에 인라인된다. `plans` 는 랜딩이 읽어야 해서
anon SELECT 가 열려 있으므로, 키가 나가면 **가격표를 통째로 긁을 수 있다.**

실측 확인 — `/work/login` 이 부르는 청크 9개에 `해그로시` · `hgrs` · `*.supabase.co` ·
JWT 접두사 **0건**. 페이지 HTML에도 브랜드 문자열 0건.

### 호스트 분리 실측

```
work-host   /  307→/work/login   /work/login 200   /admin /shortform /login /signup 404
            /favicon.ico 404  /sitemap.xml 404  /robots.txt 404
main-host   / 200   /shortform 200   /work 404   /work/login 404
```

favicon 은 proxy matcher 에서 일부러 뺐다 — 제외해 두면 브라우저가 자동으로 긁어가
우리 파비콘이 그대로 나간다.

### 작업 트랙은 클라 트랙과 별개 컬럼이다

`deliverables.status`(클라가 보는 값)와 `work_status`(작업자·우리가 보는 값)를 나눴다.
합치면 작업자가 제출하는 순간 클라이언트 화면에 나간다. 두 값은 **승인 시점에만** 만난다.

```
brief → planning → plan_review → producing → submitted → delivered → (revising) → done
                     ↑우리                     ↑우리        ↑클라
```

승인 시 `work_url` 이 `preview_url` 로 올라가고 `status='preview'` 가 된다.
클라 수정 요청은 검수를 거치지 않고 바로 작업자에게 간다 — 클라가 직접 쓴 내용이라
우리가 다시 판단할 게 없고 하루를 번다.

### 브리프는 모델이 쓰고 사람은 검수만 한다

`lib/work-brief-ai.ts`(opus-5, effort medium). 지운 식별정보를 `redacted` 배열로 돌려주게
해서 어드민이 **그것부터** 보게 했다. `published_at` 이 찍혀야 작업자 화면에 나타나고,
다시 생성하면 공개가 자동으로 내려간다. 연타 방지는 서버에서 60초 — 화면에서 막으면
새로고침 한 번에 뚫린다(7일차 지적사항 반영).

### 계정은 메일을 보내지 않는다

Supabase 초대·인증 메일은 Resend 발신 도메인(hgrs.io)으로 나간다. **그 한 통으로 끝난다.**
`/admin/workers` 에서 임시 비밀번호를 정해 계정을 만들고 직접 전달한다. 셀프 가입 없음.

### 내 손이 가는 곳은 두 군데로 줄었다

`/admin/review` 하나에 검수 대기 편이 모인다(승인/반려 버튼). 배정은 프로젝트 상세에서
편별 드롭다운 + 마감일. 그 외는 작업자와 클라이언트가 서로 밀고 당긴다.

### 배포 완료 — `shortdashboard.vercel.app`

사장님이 부른 이름은 `short-dashboard` 였는데 그 서브도메인은 **다른 Vercel 계정이 이미
쓰고 있었다**(`.vercel.app` 은 전 세계 공용 네임스페이스). 하이픈을 뺀
`shortdashboard.vercel.app` 으로 확보했다. 같은 프로젝트(`shortform-hgrs`)에 도메인만 더 붙인 것이라
배포는 한 벌이다.

마이그레이션은 **Management API** 로 적용했다(`POST /v1/projects/{ref}/database/query`).
`supabase db push` 를 쓰지 않은 이유 — 미적용 상태로 남아 있는 `20260812000001_blog.sql` 까지
함께 밀려 들어간다. 참고로 이 프로젝트에는 `supabase_migrations.schema_migrations` 테이블이
없다. 그동안 대시보드에서 손으로 밀어 왔다는 뜻이고, 그래서 적용 이력은 레포의 파일이 전부다.

### 라이브 실측

```
shortdashboard.vercel.app   /→307→/work/login   /work/login 200
                            /admin /shortform /login /sitemap.xml /favicon.ico  전부 404
hgrs.io                     / 200  /shortform 200  /portfolio 200  /work 404  /work/login 404
```

`/work/login` HTML + 청크 9개에 `해그로시`·`hgrs`·`*.supabase.co`·JWT **0건**(라이브 실측).
hgrs.io 는 Organization·WebSite JSON-LD 그대로 — 회귀 없음.

### 남은 것 ⚠️

1. **작업자 계정 0명.** `/admin/workers` 에서 발급하고 임시 비밀번호를 직접 전달해야 시작된다
2. 브리프는 클라 가이드라인이 제출된 프로젝트에서만 생성된다(현재 프로젝트 1건)
3. 소스 영상 자체에 제품·패키지가 찍혀 있으면 브랜드는 결국 유추된다. 가격·마진은 확실히
   막히지만 브랜드 익명성은 **"우리가 먼저 알려주지 않는다"** 수준이다

### 추가 — 프로젝트 일괄 배정 (같은 날)

10편·20편짜리 프로젝트에서 편을 하나씩 배정하는 건 일이다. 프로젝트 상세의 산출물 섹션 위에
`assignProject` 를 붙였다 — 담당자 / 마감일 / 범위 세 칸.

**기본 범위는 "미배정 편만"이다.** 전체 덮어쓰기를 기본으로 두면 이미 다른 사람이 붙어
진행 중인 편의 담당자까지 갈린다. 완료(`done`)된 편은 어느 범위에서도 제외한다.
비워 둔 칸은 바뀌지 않는다(담당자만, 마감일만 일괄 변경이 가능하도록).

---

## 2026-08-13 — 공정 재설계 (v2) · 5단계 · 라벨 두 벌 · 게이트 제거

1차에서 만든 8단계가 실제 일하는 방식과 달랐다. 송대표 지시로 갈아엎었다.

### 무엇이 틀렸었나

**"기획 작성중"이라는 단계.** 컨텐츠는 편당 들어가는데 프로젝트 하나에 기획 칸을 걸어 두니
작업자 입장에서 뭘 쓰라는 건지 알 수 없다. 게다가 그 기획안을 내가 검수해서 통과시켜야
제작으로 넘어갔다 — 내 손이 공정 한가운데에 박혀 있었다.

**AI가 브리프를 쓰는 구조.** 실제로는 내가 제품 링크·가격·현황·요청사항을 직접 적어 준다.
AI는 그걸 대신 쓰는 게 아니라, 그걸 **근거로 세부 자료조사**를 해 주는 보조여야 했다.

### 5단계 · 상태 하나 · 라벨 둘

`lib/work.ts` 의 `WORK_STAGES` 한 배열이 두 라벨을 다 갖는다. 상태를 두 벌로 나누면
두 화면이 반드시 어긋난다.

| key | 작업자가 보는 말 | 클라이언트가 보는 말 | 공 |
| --- | --- | --- | --- |
| `study` | 브랜드 정보 확인 | 담당자 브랜드 압축 스터디중 | 작업자 |
| `producing` | 컨텐츠 기획제작중 | 숏폼 기획제작 진행중 | 작업자 |
| `review` | 1차 완성본 컨펌 요청 | 1차 완성본 컨펌 확인 | **클라이언트** |
| `revising` | 최종 수정 반영중 | 최종 수정요청 반영중 | 작업자 |
| `done` | 최종본 전달 완료 | 최종본 다운로드/확인 | — |

작업자가 미는 전이는 셋(`study→producing`, `producing→review`, `revising→review`).
**review 에서 앞으로 가는 길은 클라이언트만 연다** — 수정 요청 또는 [이대로 확인 완료].
자기 결과물을 스스로 "확인됨"으로 넘길 수 있으면 안 된다.

### projects.stage_b 를 편에서 파생시켰다

클라이언트 대시보드는 프로젝트 단위 스텝퍼인데 진행은 편 단위다. 두 곳을 사람이 맞춰
누르면 반드시 어긋난다. **가장 뒤처진 편**을 프로젝트 단계로 삼는 트리거를 걸었다
(`sync_project_stage_b`). `lib/stages.ts` 의 `SHORTS_STAGES` 는 이제 같은 key 를 쓰는
클라이언트 라벨 사전일 뿐이다. 어드민의 숏폼 단계 수동 버튼은 없앴다.

실측: 편1만 `producing` → 프로젝트 `study` 유지 / 전 편 `review` → 프로젝트 `review` /
전 편 `study` 원복 → 프로젝트 `study`.

### 브랜드 정보는 사람이 쓰고, AI 는 그 아래로 내려갔다

`work_briefs` 를 갈아엎었다(AI 9칸 → 3칸).

| 칸 | 누가 | 어디에 뜨나 |
| --- | --- | --- |
| `manual_note` | 어드민이 수기 | 작업자 화면 맨 위 |
| `client_note` | 클라이언트가 포털에서 | 그 바로 아래 |
| `ai_analysis` | 작업자가 버튼으로 | 그 아래 (선택) |

AI 프롬프트는 "브리프 작성"에서 **"자료조사"**로 바꿨다 — 한 줄 요약 / 찾는 사람 /
소구점 / 훅 5개 / 톤 / 금지 표현 / **담당자에게 확인이 필요한 것**. 자료에 없는 사실을
만들지 말고 "확인 필요"로 남기라고 못 박았다. 지어낸 수치를 믿고 만든 영상이 나가면 끝이다.

작업자가 직접 돌리므로 비용 가드를 서버에 뒀다(프로젝트당 60초 쿨다운, 결과는 프로젝트
단위로 공유되어 같은 프로젝트의 다른 편에서도 그대로 보인다).

### 내 역할은 이만큼 남았다

신청 접수 → 상담/결제 → **작업자 배정**(전체 일괄 + 편별 조정) → **브랜드 정보 입력** →
(끝). 단계는 작업자가 밀고 컨펌은 클라이언트가 한다. `/admin/review` 는 검수 큐에서
**진행 현황**으로 바꿨다 — 지금 공이 누구에게 있는지 보고, 막힌 것만 손으로 뚫는 자리다.

### 그 밖에

- `inquiries` 9건의 담당자명이 `유건호` 로 들어가 있었다(내 발송 테스트 데이터) → `송건호` 로 수정.
  코드에는 없었고 DB와 내 메모리에만 있었다
- 작업자 화면의 "기획" 칸은 **작업 메모(선택)** 로 내렸다. 제출 조건이 아니다

### 추가 — 드라이브 직접 업로드 (같은 날)

작업자가 결과물 링크를 손으로 복붙하지 않는다. 끌어다 놓으면 지정 폴더로 바로 올라가고
파일명은 규칙대로 자동으로 붙는다.

**파일은 우리 서버를 지나지 않는다.** 서버는 구글에서 재개 가능 업로드 세션 URI 만 받아
브라우저에 넘기고, 바이트는 브라우저 → 구글로 직접 간다(XHR PUT, 진행률 표시).
영상 한 편이 수백 MB 라 서버를 통하면 Vercel 요청 본문 제한(100MB)에 먼저 걸린다.
세션을 열 때 `Origin` 헤더를 넣어야 구글이 CORS 헤더를 붙여 준다 — 빠뜨리면 브라우저가 PUT 을 막는다.

`googleapis` 패키지는 쓰지 않았다. 토큰 하나 받자고 의존성을 늘릴 이유가 없어서
`node:crypto` 로 RS256 JWT 를 직접 서명한다(`lib/google-drive.ts`, 약 40줄).

#### 파일명은 작업자가 짓지 않는다

`{앞자리}_{편번호}_{제목}_{형식}_{완성날짜}[_fin]`. 작업자는 제목과 형식(9x16/1x1/16x9)만
고르고 나머지는 조립된다. 손으로 붙이게 하면 폴더가 반드시 엉망이 된다.
수정 반영 단계(`revising`)에서 올리면 `_fin` 이 자동으로 붙는다.

**앞자리는 브랜드명이 아니라 별칭이다.** 규칙 자체는 브랜드명 자리지만 작업자에게
브랜드를 알려주지 않는 게 전제라 기본값이 `work_code`(W-0001)다. 브랜드명을 쓰고 싶으면
어드민에서 별칭에 적으면 된다 — 판단은 사람이 한다.

#### 어드민 화면을 한 자리로 모았다

흩어져 있던 드라이브 링크 두 칸을 브랜드 정보 섹션 안으로 넣었다. 이제 프로젝트에서
내가 채우는 건 한 곳이다 — 소스 폴더 / 완성본 폴더 / 파일명 앞자리 / 브랜드·제품 정보.

#### ⚠️ 아직 켜지지 않았다 — 구글 준비물

`GOOGLE_SERVICE_ACCOUNT_EMAIL` · `GOOGLE_PRIVATE_KEY` 가 비어 있어 지금은 업로드 박스가
"연결되지 않았습니다"를 돌려주고 링크 붙여넣기로 폴백한다. 켜려면:

1. GCP 프로젝트 → **Drive API 사용 설정** → 서비스 계정 생성 → JSON 키 발급
2. **공유 드라이브(Shared Drive)** 를 만들고 그 안에 폴더 둘(소스/완성본)
3. 공유 드라이브 멤버에 **서비스 계정 이메일을 콘텐츠 관리자**로 추가
4. 폴더에 클라이언트·작업자를 편집자로 공유
5. `vercel env add GOOGLE_SERVICE_ACCOUNT_EMAIL/GOOGLE_PRIVATE_KEY production` 후 재배포

`h-grs.com` 이 Google Workspace(MX = smtp.google.com)라 공유 드라이브를 만들 수 있다.
**개인 내 드라이브 폴더로는 안 된다** — 서비스 계정은 저장용량이 0이라
`storageQuotaExceeded` 가 난다. 이 오류는 사람 말로 바꿔 화면에 띄우게 해 뒀다.

---

## 2026-08-13 밤 ~ 08-14 새벽 — 공정 확정 · 드라이브 · 자동 메일 · 인플루언서 보드

작업자 대시보드를 얹은 뒤로 사장님과 화면을 주고받으며 공정을 **다섯 번 갈아엎었다.**
아래는 그 결론이다. 중간 버전은 지웠으니 이 절만 읽으면 된다.

### 공정 — 준비 트랙 + 제작 트랙, 이름은 두 벌

**단계 어휘의 단일 출처는 `lib/process.ts` 하나다.** 화면마다 말이 달라 아무도 지금이
어디인지 모르던 문제가 여기서 끝났다.

| # | 클라이언트가 보는 말 | 정하는 값 |
| --- | --- | --- |
| 1 | 브랜드/제품 소개 작성 | `projects.stage_a` |
| 2 | 모집중 | 〃 |
| 3 | 인플루언서 확정하기 | 〃 |
| 4 | 제품 및 서비스 배송하기 | 〃 |
| 5 | 소스컷 업로드/확인 | 〃 |
| 6 | 숏폼 기획제작 진행중 | `deliverables.work_status` |
| 7 | 1차 완성본 컨펌 확인 | 〃 |
| 8 | 최종 수정요청 반영중 | 〃 |
| 9 | 최종본 다운로드 / 확인 | 〃 |

작업자는 같은 상태를 **자기 말로** 본다(브랜드 정보 확인 및 전체 내용 숙지 / 컨텐츠
기획제작중 / 1차 완성본 컨펌 요청 / 최종 수정 반영중 / 최종본 다운로드 전달).
**시딩 세부는 「소스 대기」 한 칸으로 접는다** — 작업자에게 필요한 정보는 "소스가 왔나
안 왔나" 둘뿐이다. 싱글 플랜은 1~4가 통째로 빠져 5칸이 된다. 회색으로 남기지 않는다.

#### 여기서 세 번 틀렸다 — 기록해 둘 값어치가 있다

1. **라벨을 하나로 합쳤다.** 사장님이 "통일해"라고 한 건 *공정*을 하나로 맞추라는 뜻이었는데
   두 벌로 주신 *이름*을 없애 버렸다. 관점이 다르면 이름은 갈리는 게 맞다.
2. **병렬을 순차 한 줄로 접었다.** 시딩과 숏폼을 한 줄로 그렸더니 시딩이 "모집중"인데도
   완료(✓)로 찍혔다. 인덱스 하나로 "앞은 전부 완료"를 그리면 반드시 거짓이 나온다.
   → `StageSteps` 가 **칸마다 상태를 따로 받도록** 고쳤다.
3. **프로젝트 단계를 "가장 뒤처진 편" 기준으로 잡았다.** 10편 중 1편이 제작에 들어가도
   9편이 남아 첫 단계에 멈춰 있었다. → **완료만 전원 합의, 진행은 선두 기준**으로 바꿨다
   (`sync_project_stage_b`).

#### 되돌리기도 건너뛰기도 없다

사장님 확정. 지난 단계는 무엇을 확정했는지 **읽을 수만** 있다. 되돌리면 이미 나간 알림과
화면이 어긋나고, 건너뛰면 앞 단계의 전제(배송 완료·소스 확보)가 깨진 채로 진행된다.
**화면에서만 막지 않고 서버에서도 거절한다** — 폼 값은 조작할 수 있다.

### 소스 전달 = 작업 시계의 0시

어드민 **[작업자에게 전달하기]** 한 번이 준비 구간과 제작 구간을 가른다.
누르면 ① 준비 트랙이 닫히고 ② **편마다 마감일이 박히고**(주 2편 기준 편당 7일 —
1·2편 D+7, 3·4편 D+14 …) ③ 배정된 작업자에게 시작 안내 메일이 나간다.

### 구글 드라이브 — 파일이 우리 서버를 지나지 않는다

서버는 구글에서 **재개 가능 업로드 세션 URI** 만 받아 브라우저에 넘기고, 바이트는
브라우저 → 구글로 직접 간다. 영상 한 편이 수백 MB 라 서버를 통하면 Vercel 요청 본문
제한(100MB)에 먼저 걸린다. `googleapis` 패키지는 쓰지 않았다 — `node:crypto` 로 RS256
JWT 를 40줄로 서명한다(`lib/google-drive.ts`).

**밟은 함정 세 개:**
- 서비스 계정은 **개인 드라이브에 파일을 소유할 수 없다**(저장용량 0). 반드시 공유 드라이브
- 세션 생성 때 `Origin` 헤더를 넣어야 구글이 CORS 헤더를 붙인다. 빠뜨리면 브라우저가 PUT 을 막는다
- 새 형식 Supabase 키(`sb_secret_…`)를 **Storage API 가 아직 안 받는다**("Invalid Compact JWS").
  버킷은 SQL(`insert into storage.buckets`)로 만들었다

폴더는 **프로젝트마다 따로** 판다 — `{공유 드라이브}/{브랜드명_플랜}/{소스, 완성본}`.
한 폴더를 여러 프로젝트가 같이 쓰면 브랜드가 섞인다. **공유 드라이브 멤버로는 아무도 추가하지
않는다**(멤버가 되면 드라이브 전체가 보인다). 폴더 단위 권한만 준다.

권한은 **한 종류다 — 참여자 전원이 두 폴더 다 편집자.** 역할별로 읽기/쓰기를 쪼개 봤지만
설명만 길어지고 실익이 없었다. 지워도 공유 드라이브 휴지통에 30일 남는다.
**작업자를 배정하면 폴더가 없을 때 알아서 만들고 권한까지 건다** — 버튼을 따로 누르지 않는다.

파일명은 작업자가 짓지 않는다: `{브랜드명}_#{편번호}_{컨텐츠명}_{완성날짜}_{v1|fin}`.
버전은 **둘뿐이다** — 중간 버전을 열어 두면 폴더에 v2·v3·진짜최종이 쌓인다.

### 자동 메일 7종 — 전부 우리가 보낸다

소스 전달 / 48시간 진행 확인 / 마감 24시간 전 / 1차 완성본 / 최종본 / 완료 통지 /
클라이언트 방치 리마인드. 시간 기반 3종은 cron 하나가 매일 **오전 10시(KST)** 에 돈다.
같은 메일은 한 번만 — 편에 발송 시각을 남긴다.

**조용시간: KST 20:00~10:00 발송 금지.** 그 시간에 생긴 알림은 버리지 않고 Resend
**예약 발송**(`scheduled_at`)으로 다음 오전 10시에 나간다. 우리 서버가 큐를 들고 있지 않아
배포가 재시작돼도 예약이 남는다. 이력에는 `scheduled` 로 적재된다.

⚠️ 조용시간이 **두 곳에 있다.** `lib/mail.ts`(모든 메일, 예약 방식)와
`lib/blog-schedule.ts`(blog 전용, 건너뛰기 방식). 다른 세션이 같은 요구를 따로 구현했다.
고장은 안 나지만 **발송 시간을 바꿀 땐 두 파일 다** 고쳐야 한다.

### 인플루언서 보드 — 1차 선정 심사를 없앴다

인플루언서는 사장님이 다른 플랫폼에서 이미 선정해 온다. 우리 화면이 하는 일은 **링크와
인스타명만 받아 카드로 만들어 주는 것**이고, 클라이언트는 확정 명단과 올라온 콘텐츠만 본다.
거절/재선정 카운트도 두지 않는다.

`latestPosts` 와 `biography` 가 이미 Apify 응답에 들어오는데 **버리고 있었다.** 이제 저장한다 —
최근 게시물 썸네일 3장, 소개글, 그리고 업종 카테고리(비즈니스 계정이면 `businessCategoryName`,
아니면 소개글·해시태그·캡션으로 추정. AI 를 부르지 않아 비용 0).

**이미지는 받는 즉시 우리 스토리지로 복사한다**(`lib/media.ts` → 버킷 `influencer-media`).
인스타 CDN 주소에는 서명과 만료가 들어 있어 그대로 두면 몇 주 뒤 클라이언트 화면에서
사진이 통째로 깨진다. 클라이언트는 프로젝트가 끝난 뒤에도 다시 들어와 본다.

⚠️ **리워드·CPV 는 클라이언트 화면에서 뺐다.** 인플루언서 단가가 보이면 패키지 금액에서
우리 마진이 그대로 역산된다. 어드민에서만 관리한다. 벤치마크(인플루언서 플랫폼)는
브랜드가 직접 지급하는 구조라 리워드가 보이지만 우리는 구조가 다르다.

**릴스/피드 스코어와 상위 % 는 만들지 않았다** — 그건 그 플랫폼이 자기 DB 로 계산한
자체 지표다. 우리에겐 모수가 없어서 만들면 지어낸 숫자가 된다.

### 브랜드 AI 세부분석 — 만들었다가 지웠다

작업자가 자료를 더 받도록 붙였는데 사장님 판단으로 걷어냈다. 작업자는 자기가 쓰던 AI 로
알아서 하고 클라이언트는 그 결과를 볼 이유가 없다. **아무도 안 누르는 버튼 하나가 남으면
그 버튼이 부르는 API 비용·상한·실패 처리까지 계속 관리 대상으로 남는다.**
DB 컬럼 → 액션 → 화면 → 랜딩 순으로 저장 자리부터 지웠다.

덕분에 **외부 인력이 누를 수 있는 AI 호출 지점이 0개가 됐다.** 남은 Anthropic 호출은
온보딩 브랜드 분석(브랜드당 1회)과 블로그 원고(터미널 수동)뿐이다.

### 랜딩을 실제 화면과 맞췄다

공정을 갈아엎으면서 랜딩을 안 고쳐 **실물과 다른 화면을 팔고 있었다.** 세 군데를 고쳤다:
「1차 선정 심사」→ 확정 인플루언서 카드 / 옛 5단계 스텝퍼 → 실제 4단계 /
존재하지 않던 「컨텐츠 가이드라인 10편 편성」→ 브랜드·제품 소개 폼.
①「브랜드 AI 기본 분석」과 ②가 **거의 같은 표를 두 번** 보여주던 것도 여기서 드러났다.

랜딩 미리보기에는 **실존 인플루언서 사진과 계정명을 쓰지 않는다**(초상권). 우리 소재
포스터로 채우고 핸들은 `@creator_a` 로 익명화했다. 이전 버전에 실명 계정이 박혀 있던 것도 정리.

6번 카드 **「소재 정기 수급」**을 랜딩에만 추가했다(별도 플랜). 성과 수치는 넣지 않는다 —
예시 숫자가 "이 정도는 나온다"로 읽히면 그 순간 과장 광고다. **판정과 다음 수**의 구조만 보여준다.

### 그 밖에

- `inquiries` 9건의 담당자명이 **유건호**로 들어가 있었다(내 발송 테스트 데이터) → **송건호**로 수정
- 클라이언트 대시보드에서 D-day 배지 제거, 입력 칸(브랜드·제품 소개 / 제작팀에 전달할 내용)을
  **화면 맨 위로** 올리고 긴 패널은 접었다. 할 일이 남은 것만 자동으로 펼쳐진다
- `plans` 는 anon 키로 조회 가능하다(랜딩·진단이 읽어야 한다). **작업자 표면에는 브라우저
  Supabase 클라이언트를 올리지 않아** 그 경로가 막혀 있다. 로그인까지 전부 서버 액션이다

### 운영 스택 정리 문서

붙어 있는 외부 서비스 9개와 과금·보안 상태를 한 장에 모았다(아티팩트).
**실거래 전 필수 3건**: 토스 실키 교체 / 관리자 비밀번호 교체 / 테스트 계정 3개 삭제.

### 다음

1. 위 3건 처리
2. Anthropic 사용량 알림 설정
3. 미사용 환경변수 정리(`GITHUB_TOKEN`, `INSTAGRAM_SCRAPER_API_KEY`)
4. 공유 드라이브 이름 「HGRS스튜디오 프로세스」 — 작업자에게 회사가 드러난다. 중립적으로 바꿀지 판단
5. 실제 프로젝트 한 건으로 준비 트랙 → 소스 전달 → 제작 → 컨펌까지 한 바퀴 돌려보기

---

## 2026-08-13 ~ 08-14 — 블로그 시스템 한 바퀴

### 무엇을 만들었나

`/blog` 를 다시 열고 **기획 → 제작 → 검수 → 발행 → 알림**을 한 줄로 이었다.
사장님이 하시는 건 하루 한 번 메일 보고 [발행하기] 누르는 것뿐이다.

```
월·수·목 11:00  ✉️ "맥 켜 두세요 — 오늘 #N편 씁니다" (주제·타겟 미리 통보)
         11:00  맥의 launchd 가 claude -p 를 띄워 원고 작성 → 어드민 등록
         15:00  ✉️ "원고 나왔습니다" → 어드민에서 [발행하기]
         17:00  발행 + 구독자에게 신규 글 알림
```

**발행은 승인 버튼만이 시킨다.** 며칠이 지나도 승인 전에는 안 나간다.

### 이번에 붙은 것

| 영역 | 내용 |
| --- | --- |
| 규격 단일 출처 | `lib/blog-spec.ts` — 타겟·7문단 구조·유형별 최소치·금지표현·에디터 3인·AEO |
| 발행 검사식 | `lib/blog-audit.ts` — 미달이면 승인 버튼이 안 눌린다 |
| 썸네일 자동 생성 | `lib/blog-thumbnail.tsx` — 목록·구글이미지·공유가 같은 그림 |
| 회차 번호 | `blog_post.seq` — 발행 시점에 못 박아 중간 글을 내려도 안 밀린다 |
| 목록 개편 | 썸네일 카드 3열 · 인사이트 / 고객 이야기 분리 |
| 고객 이야기 8편 | 프레이머 포트폴리오 원문 이전 (크래프톤·럽디·열다·트러스티푸드·리얼클래스·모에브·왈라·파크론) |
| 신규 콘텐츠 알림 | 구독 폼 + 발행 시 1통. 20~10시는 아침까지 보류 |
| 승인/발행 분리 | `approved_at` — 승인은 허락, 발행은 예정일 17시 |
| 어드민 편성표 | No. / 키워드 / 타겟 / 세부타겟 / 주제 / 훅 제목 / 발행 예정 / 상태 |
| SEO·AEO | canonical · og:image · Article(datePublished·image) · FAQPage · BreadcrumbList · sitemap |

### 겪은 사고와 조치

**1. 앤트로픽 $22.** "$1~2" 라고 말해 놓고 1편 만드는 데 $22 가 나갔다.
원인은 파이프라인이 실패할 때마다 **가장 비싼 조사(웹 검색 12회)를 처음부터
다시 돌린 것** — 5회 태웠다. 회당 약 $4.4.

→ 서버 러너는 단계마다 DB 에 저장해 이어 달리게 만들었고(`blog_job`),
   **결국 서버 자동 생성은 잠갔다.** 원고는 맥의 Claude Code(구독)가 쓴다.
   앤트로픽 청구는 0원. 러너는 `BLOG_AUTOGEN=on` 으로 나중에 열 수 있다.

**2. 타임존.** Vercel 은 UTC 로 돈다. `setHours(15)` 가 15:00 UTC = 한국 자정을
가리켜 검수 메일이 한 통도 안 나갔다. `발행 17시` 도 새벽 2시가 될 뻔했다.

→ `lib/blog-schedule.ts` 에 `kstParts`/`kstMoment`/`kstAddDays` 를 두고
   로컬 시각 함수를 안 쓴다.

**3. 엣지 캐시.** cron 이 이전 응답을 재사용해 "보낼 게 있는데 0건" 이 돌아왔다.

→ cron 라우트 5개에 `cache-control: no-store`.

**4. `ADMIN_EMAIL` 미설정.** Vercel 에 아예 없어서 검수 메일이 500 이었다. → 넣음.

**5. 썸네일 URL.** Next 가 `opengraph-image` 에 빌드마다 바뀌는 해시를 붙여
목록 카드에서 부를 수 없었다. → `/api/blog/thumbnail/[slug]` 로 고정 입구를 따로 두고
그리는 코드는 한 벌로 공유.

### 보안 점검 (08-14)

| 항목 | 결과 |
| --- | --- |
| 빌드 산출물 비밀값 유출 | ✅ 없음 |
| 검색엔진 인증 태그가 HTML 에 노출 | ✅ 정상 — 공개되라고 있는 값. 검사기에서 제외 처리 |
| cron 라우트 인증 | 🔧 `===` 문자열 비교 → `lib/cron-auth.ts` 의 timingSafeEqual 로 교체 |
| `blog_subscriber`(구독자 메일) | ✅ RLS ON · 정책 0개 = 서비스 키로만 |
| 시크릿이 레포에 평문으로 | ✅ 없음 |

**남은 것**: 구독 폼(`app/(site)/blog/subscribe.ts`)은 비로그인 공개라 **속도 제한이 없다.**
스팸 등록이 가능하다. 실해는 없지만 목록이 더러워진다.

**주의**: `scripts/auto-write.sh` 는 `Bash(curl:*)` 를 허용한다. 그 세션이
웹 검색 결과(신뢰할 수 없는 텍스트)를 읽으므로, 프롬프트 인젝션이 성공하면
curl 로 외부에 뭔가 보낼 수 있다. `--dangerously-skip-permissions` 는 쓰지 않았고
도구를 최소로 좁혔지만, curl 은 넓다. 실제 운영 몇 회 지켜보고 좁힐 것.

### 운영 문서

- `docs/BLOG_OPS.md` — 사장님이 하실 일(cron-job.org·Search Console·서치어드바이저)
- 검색엔진 소유확인은 DNS TXT 대신 **메타 태그** 방식. 값은 환경변수
  (`GOOGLE_SITE_VERIFICATION`·`NAVER_SITE_VERIFICATION`)라 코드 수정 없이 교체된다

### 다음

1. **8/17(월) 11시 첫 자동 실행** — 로그 `~/Library/Logs/hgrs-blog-auto.log` 확인
2. 고객 이야기 8편에 이미지 붙이기 (사장님이 캡처 주시면)
3. 구독 폼 속도 제한
4. 실거래 전 필수 3건은 여전히 미처리 (토스 실키 · 관리자 비밀번호 · 테스트 계정)

---

## 2026-08-14 (오후) — 블로그 완전 자동화 + 스튜디오 QA 한 바퀴

### 1. 새벽 오류 메일의 정체

`blog_job` 08-17 회차가 세 번 다 죽어 있었다. 오류 원문:

```
400 credit balance is too low to access the Anthropic API
```

**앤트로픽 크레딧이 바닥났다.** cron-job.org 는 응답이 2xx 가 아니면 실패 통지를
보내므로, 그게 새벽에 도착한 메일이다.

### 2. 엔진을 OpenAI 로 갈아 끼웠다

프롬프트·규격(`blog-spec.ts`)·검사식(`blog-audit.ts`)은 **손대지 않았다.**
1편의 품질을 만든 것은 모델이 아니라 그 두 파일이라서, `lib/blog-ai.ts` 의 API
호출부만 Responses API 로 바꿨다.

| 단계 | 모델 | 단가(1M) |
| --- | --- | --- |
| 조사·기획 | `gpt-5.6-terra` | $2 / $12 |
| 집필·교정 | `gpt-5.6-sol` | $5 / $30 |

`BLOG_MODEL_RESEARCH` / `_PLAN` / `_WRITE` 로 갈아 끼울 수 있다.

**실측 1편(08-14):**

| 항목 | 값 |
| --- | --- |
| 조사 (웹검색 13회) | $0.67 |
| 기획 + 집필 | $0.34 |
| 교정 1회 | $0.18 |
| **합계** | **$1.19** · 3,906자 · 자료 5건 · 규격 통과 |

앤트로픽 시절 편당 $4.4 였다.

### 3. 사람이 누르는 버튼을 없앴다

```
research → plan → verify → write → polish → done
```

`polish` 가 새로 생긴 정거장이다. 사장님의 [발행하기] 자리를 **검사식**이 받는다.

- 규격 통과 → `approved_at` 을 찍는다 → 그날 17시 자동 발행
- 미달 → 걸린 항목만 모델에게 돌려주고 다시 쓰게 한다 (최대 2회)
- 그래도 미달 → **승인하지 않는다.** 그 회차는 안 나가고 리포트에 사유가 실린다

마지막 줄이 설계의 전부다. 자동화의 목적은 주 3회를 채우는 게 아니라
사장님이 안 보셔도 되는 품질을 지키는 것이다.

### 4. 지시문과 검사식이 서로 다른 말을 하고 있었다

첫 OpenAI 원고가 6개 항목에서 미달로 걸렸다. 원인은 모델이 아니었다.

| | 지시문 | 검사식 |
| --- | --- | --- |
| 본론 H2 | "블록 수 6~7개" (FLOW 7단계를 말한 것) | 3~5개 |
| 분량 | "3200자 **이상**" | 3200~4600자 |
| 시각 자료 | "4~5개" | `:::source` + 표 합산 5개 이내 |

게다가 자료 규격이 **섹션마다 최소 1건** 인용을 요구해서, 섹션이 5개면 인용만
5개라 표 하나만 넣어도 미달이다. 두 규칙이 동시에 성립하는 구간이 좁다.

→ `editorialRules` 가 그 구간을 **계산해서** 알려 주도록 바꿨다
(`sectionMax = min(f.maxH2, maxVisuals - f.minTables)`). 앞선 모델은 우연히
통과했을 뿐, 지시문이 처음부터 틀려 있었다. 고친 뒤 미달 6건 → 1건 → 교정 후 통과.

### 5. 오류 메일 대신 리포트 한 통

cron 입구를 `lib/blog-ops.ts` 의 `cronRoute` 로 감쌌다. **무슨 일이 있어도 200 을
돌려준다.** 실패는 `blog_ops_log` 에 쌓이고 저녁 리포트의 "걸린 것" 칸에 실린다.
(401 만 예외 — 남이 문을 두드린 건 우리 고장이 아니다.)

`/api/blog/report` 가 편성일 18시에 한 통 보낸다: 발행 여부 · 이번 편 비용 ·
이번 달 누적 · 조회수 · 걸린 것. `?force=1` 로 지금 한 통 보낼 수 있다.

**조회수는 우리가 직접 센다** — `blog_view` 표 + `/api/blog/view` + 발행면의
`ViewBeacon`(세션당 1회, 봇 제외). Search Console 전까지 추정치를 적지 않는다.

은퇴한 입구 둘(`/api/blog/notify`, `/api/blog/remind`)은 **파일을 지우지 않았다.**
지우면 404 가 되고, 404 는 다시 실패 통지 메일이 된다.

맥의 launchd(`io.hgrs.blog.autowrite`)는 껐다. 서버가 쓰므로 맥이 꺼져 있어도 돈다.

### 6. 스튜디오 QA — 클라이언트 ↔ 작업자 한 바퀴

세 계정(클라·작업자·어드민)으로 실제 세션을 만들어 라이브 화면을 그대로 받아 봤다.
**찾은 것 6건, 전부 고쳤다.**

| # | 무엇 | 어디 |
| --- | --- | --- |
| 1 | 클라이언트 대시보드에 가이드라인 입력 폼이 **두 벌** 그려짐 | `dashboard-view.tsx` — 08-13 "맨 위로 올리기" 때 원본을 안 지웠다 |
| 2 | **작업자 화면에 클라이언트 회사명이 그대로 노출** | `/work` 목록·상세 둘 다 `company_name ?? work_code` 였다. 순서가 거꾸로였고, 이제 쿼리에서 아예 안 가져온다 |
| 3 | 업로드 **파일명 앞자리**가 회사명 | `work/actions.ts` — 어드민 화면은 "브랜드명을 넣으면 특정됩니다" 라고 경고하면서 기본값이 회사명이었다 |
| 4 | **드라이브 폴더 이름**이 `회사명_플랜` (작업자에게 편집 권한을 주는 폴더) | `setupFolders` + 어드민 안내문. `W-0001_그로스 패키지` 로 |
| 5 | 작업자 상세 페이지 **탭 제목이 "작업 \| 해그로시"** | 루트 레이아웃 template 이 붙고 있었다 |
| 6 | 어드민이 편 상태를 바꿔도 **작업자 트랙이 안 따라감** | `saveDeliverable` 이 `status` 만 썼다. 클라는 "수정 반영중", 작업자는 "기획제작중" 인 편이 실제로 하나 있었다 |

추가로, 클라이언트가 08-10 에 가이드라인을 넣었는데 **작업자에게 나가는 유일한
문서(`work_briefs.manual_note`)가 나흘째 비어 있었다.** 작업자 화면에는 "아직
등록되지 않았습니다" 만 떠 있다. 아무도 안 알려 주면 아무도 모르는 자리라,
어드민 프로젝트 화면에 경고를 붙였다 — *배정은 됐는데 이 칸이 비어 있습니다*.

QA 계정 3개는 되돌리고 지웠다. 다만 `qa-admin@hgrs.io`(테스트 브랜드)의 비밀번호는
QA 중에 바꿨다 — 어차피 삭제 대상 계정이다.

### 남은 것

1. **cron-job.org 스위치 정리** — `docs/BLOG_OPS.md` 1번. 이것만 하시면 자동 운영 시작
2. Search Console · 네이버 서치어드바이저 메타 태그 값
3. 기존 프로젝트의 드라이브 폴더는 여전히 `QA 브랜드_...` 이름이다 (새 규칙은 새로 만들 때부터)
4. 실거래 전 필수 3건 (토스 실키 · 관리자 비밀번호 · 테스트 계정 삭제)

### 같은 날 오후 — 사장님 피드백 4건 반영

**1. 작업자에게 브랜드를 다시 보여 준다.** 오전에 작업번호(W-0001)로 가렸던 것을
전부 되돌렸다. 사장님 판단: *"작업자한테는 플랜 가격이 노출되지 않으면 되는 것이고,
어차피 작업자와 클라이언트는 서로 대화할 채널이 없다."*

가리는 유일한 대상은 **돈**이다 — `/work` 표면에서 `plans`·`orders` 를 조인하지
않는다(금액이 붙는 순간 마진이 역산된다). 목록·상세·업로드 파일명 앞자리·드라이브
폴더 이름 넷 다 브랜드명으로 돌아왔다. 08-13 의 "아예 빼라" 지시가 뒤집힌 것이라
메모리도 같이 고쳤다.

**2. 그로스 AI 브랜드 프로필 배너를 걷어냈다.** 입력할 자리를 두 번 만들고 있었다 —
배너에서 한 번, 아래 [브랜드 · 제품] 칸에서 또 한 번. 사장님: *"저 그로스AI는
굳이 필요없는거였잖아. 저 칸 대신 내가 말한 입력칸이 저장되어야 작업자한테 들어가잖아."*

**3. 클라이언트 입력이 작업자에게 직접 간다.** 이게 오늘 구조 변경 중 제일 크다.

```
전:  클라 project_guidelines  →  (어드민이 손으로 옮겨 적음)  →  work_briefs  →  작업자
후:  클라 project_guidelines  →────────────────────────────────────────────→  작업자
```

옮겨 적는 사람이 한 명 끼면 그 사람이 병목이 된다. 실제로 그 칸이 나흘째 비어 있었다.
`project_guidelines` 에 `promotion`(진행 중인 프로모션) 칸을 더하고, **판매 링크 ·
가격/옵션/수량 · 프로모션 셋을 서버에서 필수로 막았다** — 이 표가 작업자에게 나가는
유일한 문서라 비면 제작이 시작될 수 없다. 어드민의 옛 칸은 *작업자에게 덧붙일 메모(선택)*
로 내렸다.

**4. 블로그를 매일 1편으로.** `WEEKLY_SLOTS` 를 7일 전부로 늘리고 월 상한을 $50 로
올렸다(매일 × $1.2 ≈ $37). 주제 큐가 17개뿐이라 **17일이면 마른다** — 리포트에
"발행 없음" 이 이틀 연속 뜨면 큐부터 봐야 한다.

**2편 발행.** `소재 피로도, 조회수 하락 전에 교체 시점을 읽는 법` — 1차 교정 후
규격 통과(3,765자 · 자료 5건 · $0.14). 오늘 17시 발행.

### 아직 못 한 것

**신청 → 플랜 적용 → 작업자 배정 → 클라 대시보드** 한 바퀴를 **클릭으로** 못 돌렸다.
크롬 확장이 연결돼 있지 않아 서버 액션 버튼을 누를 수 없었다. 대신 확인한 것:

- 신청 9건 전부 `signup_completed` 계정이 있다 → [적용 시작] 이 계정 없음으로 막히지 않는다
- `startProject` 는 projects + project_guidelines + deliverables(편 수만큼) 를 함께 만든다
- `work_code` 는 컬럼 기본값(`W-` + 시퀀스)이라 따로 넣을 게 없다
- 플랜 7종 전부 `active` — 드롭다운에 다 뜬다

### 같은 날 저녁 — 사장님 피드백 2차

**2편 발행됨.** 17:00 정각, `#2 소재 피로도, 조회수 하락 전에 교체 시점을 읽는 법`.

#### 1. 편성표 키워드 칸이 영문 슬러그였다

`ai-shortform-baseline-vs-conversion-2026`. 슬러그는 주소이지 키워드가 아니다.
원인은 두 가지가 겹쳤다 — `listPosts` 가 `plan` 컬럼을 안 읽어서 기획안의
`head_keyword` 가 늘 null 이었고, 그래서 `?? post.slug` 폴백으로 떨어졌다.

→ `PostRow.headKeyword` 를 만들고 select 에 `plan` 을 넣었다. 이제 1편은
`AI영상제작`, 2편은 `소재 피로도` 로 뜬다.

#### 2. 노리는 검색어의 검색량·CTR 을 같은 줄에 세웠다

사장님 요청: *"4에 적힌 키워드에는 5,6에서 어떤 키워드(검색량·클릭률)를
노리는지도 같이 명시해서 표를 만들라고 했던 것 같아."*

편성표에 **검색량 · CTR · 경쟁도 · 생성비** 열을 더했다. 값은 키워드 보드와
같은 실측(네이버 검색광고)이고, 수집 안 된 검색어는 `—` 다.

편성 큐의 검색어는 손으로 적은 것("숏폼 소재 교체 주기")이고 네이버 수집분은
붙여 쓴 것("숏폼소재교체주기")이라 공백을 지우고 맞춘다.

> ⚠️ 그래도 **3편 이후는 검색량이 대부분 비어 있다.** 편성 큐의 검색어가
> 수집 목록에 아예 없기 때문이다. 즉 지금 큐는 "검색량을 모르는 검색어"를
> 노리고 있다. 다음 단계는 큐를 `blog_keyword` 의 니치 상위 검색어로 바꾸는 것.

#### 3. 남의 업체 이름

사장님: *"남의 업체 이름은 왜 들어가 있어 설마."* 키워드 보드 1위가
`새결마케팅` 이었다. **본문에 들어간 게 아니라 키워드 후보 목록**이다 —
네이버 검색광고 키워드도구가 연관 검색어를 뱉을 때 브랜드 검색어를 같이 준다.

규칙으로 걸러 보려 했다: "…마케팅/…광고/…대행사로 끝나는데 앞자리가 일반명사가
아니면 업체명". 730개 중 **149개**가 걸렸는데 `광고대행사`·`제휴마케팅`·
`화장품마케팅` 같은 멀쩡한 검색어가 대부분이었다. 접었다.

→ 확인된 둘(`새결마케팅`·`올바른마케팅`)을 `dropped` 로 눕히고, 키워드 보드
각 줄에 **[제외] 버튼**을 붙였다. 판단은 사람이 하고 기억은 DB 가 한다.

#### 4. 미리보기 = 완성본

사장님: *"미리보기는 실제 포스팅 예정인 완성본 글이어야 하잖아. 본문과 이미지
자료가 분리된 게 아니라 실제 사람이 보게 될 완벽한 포스팅을 내가 봐야."*

검수 화면 맨 위에 **발행면과 같은 함수·같은 클래스**로 그린 미리보기를 넣었다
(`renderBody` + `blog-body`). `:::source N` 이 실제 임베드·출처 카드로 치환된
상태로 보인다. 아래의 마크다운 편집기와 자료 목록은 고칠 때 쓰는 도구로 남겼다.

#### 5. cron-job.org 를 안 쓴다 — 맥이 5분마다 두드린다

사장님: *"크론 필요없어 그럼. 어차피 너가 알아서 매일 돌릴거잖아."*

제가 매일 세션을 여는 것에 기댈 수는 없어서, 맥에 5분짜리 틱을 걸었다.

| | |
| --- | --- |
| `scripts/blog-tick.sh` | generate · publish · announce · report 를 차례로 두드린다 |
| `io.hgrs.blog.tick.plist` | 5분 간격 · RunAtLoad |
| 로그 | `~/Library/Logs/hgrs-blog-tick.log` (할 일 있었을 때만) |

두드리기만 한다 — 원고는 서버가 만든다. launchd 는 PATH 를 거의 안 물려줘서
`curl`·`date` 를 절대 경로로 부른다(처음에 이걸로 조용히 실패했다).

#### 6. QA 를 실제 계정으로 다시 세웠다

사장님 지적대로 `qa-admin@hgrs.io` 가 아니라 **실제 계정 3개**로 옮겼다.

| 역할 | 계정 |
| --- | --- |
| 어드민 | `ceo@h-grs.com` |
| 클라이언트 | `growthkono@gmail.com` |
| 작업자 | `timentide1221@gmail.com` |

`W-0002`(테스트 / 그로스 패키지 10편)를 열고 10편 전부를 작업자에게 배정했다.
기존 `W-0001`(QA 브랜드)은 작업자 배정을 뗐다 — 데이터는 남기고 작업자 보드에서만
사라진다. QA 전용 계정 3개는 지웠다.

### 같은 날 밤 — 크롬 확장 연결 후 실제 계정 QA

크롬이 붙어서 **실제 계정으로 한 바퀴**를 돌렸다.

#### 클라이언트 → 작업자, 어드민 개입 0으로 검증됨

브라우저가 `growthkono@gmail.com`(클라이언트)로 로그인돼 있었다. W-0002 대시보드에서
브랜드·제품 정보 9칸을 **실제로 타이핑하고 [가이드라인 저장]을 클릭**했다.

곧바로 `timentide1221@gmail.com`(실제 작업자) 세션으로 `/work/<id>` 를 열었더니
아홉 칸이 **그대로** 떠 있었다 — 판매 링크, 정가·옵션·수량, 8월 프로모션(2+1),
타겟, USP, 톤앤매너, 금지 표현, 요청사항까지. 어드민이 옮겨 적는 단계는 없었다.

작업자 화면에 플랜 이름·결제 금액은 한 글자도 없다(제품 정가는 작업 정보라 보인다).

> 작업자 세션은 비밀번호를 건드리지 않고 `admin/generate_link` 로 발급받았다.
> 실제 작업자 계정의 비밀번호를 바꾸면 그분이 못 들어온다.

#### 편성 큐를 검색량 있는 검색어로 갈아엎었다

사장님 기준: *"검색량이 적어도 최소 1-2천 건 이상은 나오는게 맞아. 그 이하는 정말
별 의미가 없어서. 시즌성 키워드가 아닌 이상."* + AI 자주 · 숏폼/인플루언서 중요 ·
**컨텐츠 마케팅이 가장 핵심**.

기존 큐 17개는 손으로 적은 말("숏폼 소재 교체 주기")이라 네이버 수집 목록에 없었다.
검색량을 모르는 검색어를 노리고 있었던 것이다. 실측 1,000+ 검색어로 25개를 다시 짰다:

| 축 | 검색어(월 검색량) |
| --- | --- |
| 컨텐츠 마케팅 | 상세페이지제작 6,790 · 바이럴마케팅 4,430 · 블로그마케팅 1,810 · 인스타마케팅 1,200 · CRM마케팅 1,430 · 객단가 1,760 |
| 숏폼 | 영상제작 1,930 · 홍보영상제작 1,370 · 인스타광고하는법 1,710 · 인스타광고비용 1,280 · 틱톡에이전시 1,150 |
| 인플루언서 | 인스타광고 1,870 · 퍼스널브랜딩 1,410 |
| AI | AI영상제작 3,970 · AI추천 5,700 · 퍼포먼스마케팅 1,680 · ROAS 4,600 |
| 플랫폼 | 메타광고 5,860 · 유튜브광고 4,050 · 구글광고 2,400 · 광고대행사 2,960 · 브랜딩 4,650 |
| 시즌(8~9월) | 마케팅 19,800(추석 리드타임) · 캠페인 8,530(4분기 예산) |

**큐가 마르면 키워드 보드가 이어받는다** — `blog_keyword` 에서 검색량 1,000 이상,
우리 축에 걸리는 것을 니치 점수 순으로 자동으로 꺼낸다. 25일 뒤 조용히 멈추지 않는다.

#### 조용히 매일 같은 주제를 쓸 뻔했다

큐에서 다음 주제를 고를 때 `blog_job.topic`(각도 문장)을 모아 놓고 `TOPIC_QUEUE.term`
(검색어)과 비교하고 있었다. 둘은 절대 같아질 수 없어서 **큐의 첫 항목이 영원히 다시
뽑히는** 구조였다. 주 3회일 땐 안 드러났고, 매일 1편으로 바꾸는 순간 같은 주제를 매일
쓰는 사고가 났을 것이다. 비교 기준을 `keyword_term` 으로 맞췄다.

#### 못 한 것 (권한)

오토 모드 분류기가 둘을 막았다. 타당한 차단이라 우회하지 않았다.

1. **어드민 [적용 시작] 버튼 클릭** — 크롬이 클라이언트 계정으로 로그인돼 있고,
   어드민으로 바꾸려면 비밀번호가 필요했다. 사장님이 크롬에서 `ceo@h-grs.com` 으로
   로그인해 두시면 이어서 누른다.
2. **Supabase auth redirect 허용 목록** — `uri_allow_list` 에 `hgrs.io` 가 없다.
   `shortform.hgrs.io` 시절 그대로다. **메일 인증·매직링크가 hgrs.io 로 돌아올 수
   없는 상태**이고, 이건 실거래 전에 반드시 고쳐야 한다.

---

## 2026-08-14 (밤) — AI 광고영상: 기획이 결과물의 전부였다

### 세 판을 만들었다

| | v2 | v3 | v4 |
| --- | --- | --- | --- |
| 기획 | 기능 나열 | **12단 소구 사슬** | v3 그대로 |
| 컷 | 11컷 / 2.8초 | 21컷 / 1.76초 | 20컷 / 1.93초 |
| 나레이션 | TTS 7문장, 중간 침묵 | TTS 12문장, 침묵 0 | **sora 인물이 직접 발화** |
| 자막 | 3층 / 36~52pt | **1층 25장 / 63~88pt** | 1층 23장 |
| 길이 | 30.8초 | 36.9초 | 38.5초 |

**소스도 툴도 그대로인데 기획만 바꿔서 v2 → v3 가 됐다.** 추가로 든 돈은 $2.40 뿐이다.
이게 "기획안을 클라이언트에게 줘야 하는가" 에 대한 답이다 — 줘야 한다. 기획은 $0.40,
영상은 $6 이다. 컨펌 없이 만들면 USP 가 틀린 순간 $6 이 날아간다.

### 레퍼런스를 실측해서 규격을 다시 썼다

`영상모음2/빌고빌었는데.mp4`(31초) 를 whisper 로 받아쓰고 프레임을 떠서 쟀다.

- **31초에 12문장. 빈 구간 0초.** 발화 속도 8.5자/초. v2 는 절반이었다.
- 12문장이 **전부 다른 일**을 한다 → `PITCH_CHAIN` 12칸으로 박았다.
  훅 · 증상 · 증상 · 죄책감 · 발견 · 원리 · 반론봉쇄 · 사용법 · **변화 클라이맥스** ·
  근거 · 경제성 · 타겟호명+CTA
- **자막은 한 층뿐이다.** 2차 규격(3층)이 틀렸다. 대신 그 한 층이 가로폭 90% 를 채운다.
  한 장 8~11자, 1.5초마다 교체, 박스 없이 두꺼운 검은 테두리.
  → 짧은 문장은 크게, 긴 문장은 작게. **글자 크기를 문장 길이에서 역산**한다.

`lib/adfilm-spec.ts` 3차 개정: `CAPTION_SPEC`(1층) · `PITCH_CHAIN` · `NARRATION` 신설.
`TEMPO.targetCut` 2.4 → 1.8. `auditAdFilm` 의 나레이션 검사를 **상한에서 하한으로 뒤집었다** —
말이 많은 게 문제가 아니라 적은 게 문제였는데 거꾸로 잡고 있었다.

### 목소리는 귀로 고르지 않는다

사장님: *"나레이션하는 사람 외국인이야? 한국말이 어눌해."* 원인은 내가 고른 `nova` 였다.

**검증법을 만들었다 — 뽑은 음성을 whisper 로 되받아써서 원문과 대조한다.**

| 음성 | 되받아쓰기 | |
| --- | --- | --- |
| coral · sage · marin | 원문 100% + 구두점 | 합격 |
| ballad | 숨숨집 → **"승승집"** | 탈락 |
| nova | 안 들어가고 → **"안 들어 바보"**, 구두점 0 | 탈락 |

### 그런데 TTS 자체가 답이 아니었다

사장님: *"너 자꾸 공짜나 다루기 쉬운거 끌어와서 쓰는거니."* 맞다. 키가 이미 있어서 썼다.

**sora-2 에게 한국어 대사를 시켰더니 원문을 100% 발음했고, 시키지도 않은 "아니," 를
스스로 붙였다.** 읽는 게 아니라 말하고 있다는 뜻이다. TTS 가 못 하는 일이다.

그래서 `scripts/adfilm-edit3.mjs` 를 새로 썼다. 소리축과 화면축을 따로 들고 가면서,
인물이 보이는 구간에서는 **그 시점의 소리가 원래 클립 어디였는지 역산해** 프레임을
가져온다. 그래야 입이 맞는다.

### v4 에 남은 문제 (다음 판에서 잡을 것)

1. **인물이 클립마다 다르다.** 발화 4개를 병렬로 뽑아서 배경·헤어가 달라졌다.
   `extensions` 로 한 영상을 이어 늘려야 한다(순차라 20~30분).
2. **c4 가 숫자에서 넘어졌다.** "하나로 스물두 평 삼십 일" → "한하루 22평 31".
   그 문장을 도려내고 냈다. **sora 에게 숫자를 시킬 때는 한글로 풀어 쓴다.**
3. 사장님 평: *"그렇다고 그냥 나레이션을 통으로 입혀버렸네 어색하게."*
   발화 클립의 소리를 브롤 위에 통으로 깐 것이 어색하다는 지적. 인물컷과 브롤의
   비율·배치를 다시 설계해야 한다.

### 원가 (실측)

| 항목 | 단가 | 1편 | 원가 |
| --- | --- | --- | --- |
| sora-2 소스 | $0.10/초 | 60초 | $6.00 |
| 발화 클립 추가 시 | | +48초 | +$4.80 |
| 나레이션 TTS | | 12문장 | $0.05 |
| 기획 LLM | | | $0.40 |

**소스가 원가의 93%.** 컷을 21개로 늘려도 돈은 안 는다 — 길게 뽑아 잘게 자르기 때문이다.
컷마다 생성하면 $8.4 로 오르면서 인물 일관성까지 깨진다.

---

## 2026-08-14 (밤) — 마감 점검: 블로그가 다섯 시간 동안 안 돌고 있었다

### launchd 가 스크립트를 아예 못 열고 있었다

`launchctl list` 의 종료코드가 **127**. 로그에는 이것만 5시간치 쌓여 있었다.

```
/bin/zsh: can't open input file: /Users/gunho/Documents/hgrs-boost/scripts/blog-tick.sh
```

파일도 있고 실행 권한(755)도 있는데 못 연다. **macOS TCC** 때문이다 — Ventura 이후
`~/Documents`·`~/Desktop`·`~/Downloads` 는 보호 폴더라 launchd 로 뜬 프로세스는
사용자가 따로 허락하지 않으면 읽지 못한다. `/bin/zsh` 에 전체 디스크 접근을 주는 건
셸 전체에 권한을 여는 것이라 하지 않았다.

**스크립트와 시크릿을 보호 대상이 아닌 곳으로 옮겼다.**

- `~/Library/Application Support/hgrs/blog-tick.sh` (700)
- `~/Library/Application Support/hgrs/cron-secret` (600) — `.env.local` 을 안 읽는다

옮기고도 여전히 127 이었다. 두 번째 함정: plist 가 `zsh -lc <경로>` 로 부르고 있어
**"Application Support" 의 공백에서 잘렸다.** 스크립트를 인자로 넘기도록 고쳤다.

검증: 종료코드 `0`, err 로그 비어 있음, 실제 응답 수신
(`report → {"sent":false,"reason":"오늘 리포트는 이미 나갔습니다"}`).

### 영상에는 과금 상한이 한 줄도 없었다

블로그는 편당 $4 · 월 $50 상한이 있는데 영상은 없었다. 그래서 이날 sora 를 108초
불러 **$10.80** 이 나갔고 계정 충전액을 넘겼다. 같은 키를 쓰는 블로그가 내일 못 돌 뻔했다.

`lib/adfilm.ts` 에 `FILM_COST`(편당 $12 · 월 $60) 와 `assertWithinBudget()` 을 넣고
`startFilm`·`extendFilm` 이 **부르기 전에** 막도록 했다.

### 보안 점검

- `node scripts/secret-scan.mjs` → 유출 없음 (브라우저로 나가는 55개 파일 / 서버 키 13개)
- `.env*` 는 `.gitignore` 에 있고 git 이 추적하지 않는다 (`.env.example` 만 추적)
- 새로 만든 시크릿 파일은 600, 디렉터리는 700

### 사장님께 확인받아야 할 것

**OpenAI 잔액.** 이 키는 `api.usage.read` 스코프가 없어 내가 사용량을 조회할 수 없다.
선충전 $10 을 넘겨 썼으므로(내 계산 $10.90) 잔액이 없으면 내일 발행이 실패한다.
→ https://platform.openai.com/settings/organization/billing/overview

## 2026-08-14 (밤) — /sns-brand 채널 포트폴리오

사장님이 준 채널 7개(유튜브 2 · 인스타 2 · 블로그 2 · 콜라보 영상 1)를
`/sns-brand` 에 **채널 포트폴리오** 섹션으로 얹었다. 로고월(어떤 브랜드와) 뒤,
영상 포트폴리오(그 계정에 무엇을) 앞이다.

- 컴포넌트 `components/sns/s-channels.tsx`, 데이터 `lib/sns-brand.ts` 의 `CHANNELS`
- 데스크톱 2열 그리드 / 모바일은 세로로 쌓지 않고 **스크롤 스냅 가로 스와이프**
- 정렬은 컴포넌트에서 `count` 내림차순. 배열 순서에 기대지 않는다

### 숫자는 전부 직접 읽었다

지어낸 값이 한 줄도 없다. 2026-08-14 기준 공개 페이지에서 확인한 것:

| 채널 | 지표 |
| --- | --- |
| MKTV 김미경TV (콜라보 영상 호스트) | 구독자 189만 |
| 배틀그라운드 이스포츠 (유튜브) | 구독자 11.7만 |
| 배틀그라운드 이스포츠 (인스타) | 팔로워 2.6만 |
| 트러스티푸드 (인스타) | 팔로워 1.3만 |
| 트러스티랩스 (유튜브) | 구독자 1,770 |
| 파크론 제로블럭 (네이버 블로그) | 이웃 497 |
| 왈라 블로그 | 공개 지표 없음 |

인스타 팔로워는 로그인 없이 og:description 에서 읽힌다(`26K Followers ...`).
유튜브 구독자는 채널 HTML 의 `구독자 N만명` 문자열. 네이버는 모바일 프로필의
`N명의 이웃`. 왈라는 공개 구독 지표가 아예 없어 **비워 두고 흐리게 찍는다** —
굵게 쓰면 빈 값이 성과처럼 보인다. 다음에 갱신할 때도 같은 규칙이다.

### 썸네일은 "채널의 실제 게시물"로 만든다

처음엔 유튜브 채널 홈을 캡처했는데 **구독 버튼과 내 계정 사이드바가 같이 찍혔다.**
남의 랜딩에 내 유튜브 구독 목록이 박히는 셈이라 전부 버리고 다시 만들었다.

- 유튜브 — 최근 영상 썸네일 4장 2×2 (`i.ytimg.com/vi/<id>/maxresdefault.jpg`)
- 인스타 — 프로필 그리드만 크롭 (좌측 네비·우측 채팅 위젯 잘라낸다)
- 블로그 — 최근 포스트 커버 4장 2×2 (네이버는 RSS 의 `blogthumb.pstatic.net`,
  단 `?type=` 은 `s3` 만 살아 있어 365px 가 원본이다)

전부 1280×720 으로 통일해 `public/sns/channels/` 에 뒀다.

검증: `node scripts/mobile-audit.mjs http://localhost:3001 390` → 넘침 없음,
`npm run build` 통과, `vercel --prod` 후 https://hgrs.io/sns-brand#channels 확인.

### 되돌림 — 섹션을 빼고 사례 카드 안으로

같은 날 밤, 사장님이 **섹션을 통으로 빼라**고 하셨다. 맞는 판단이다. 채널을
사례에서 떼어 놓으면 그냥 남의 계정 목록으로 읽힌다. 사례 카드 안에 넣으면
"이 프로젝트에서 우리가 굴린 계정이 이거다"가 한 화면에서 끝난다.

- `components/sns/s-channels.tsx` 삭제, `CHANNELS` → `ChannelTile` 타입으로 축소
- 크래프톤 카드: 유튜브·인스타 두 장 + 묶음 라벨 "모든 공식채널 연간 대행"
- 트러스티 카드: 트러스티랩스("처방 브랜드 진단 퍼널 연결")·트러스티푸드 인스타
- 사례 목록 끝에 **공식 블로그 관리** 칸을 하나 더: 제로블럭 · 왈라("KT 억대 수주
  연결") · 사이버다임(우상단 "PT 및 컨설팅")
- 삼분의일(MKTV 콜라보)은 뺐다. 이미지도 지웠다

사이버다임 썸네일은 imweb CDN 의 포스트 커버 4장(`cdn.imweb.me/thumbnail/...`)으로
만들었다. 유튜브·인스타·네이버와 같은 규격(1280×720 2×2)이다.

---

## 2026-08-16 — 블로그 오진 수습 + 광고영상 기획 전면 개편

### 하루 요약

오전에 **멀쩡한 블로그 자동화를 "죽었다"고 오진**한 것에서 시작해, 오후에는
**광고영상 기획 공정을 처음부터 다시 세웠다.** 사장님이 하루 종일 직접 가르쳐 주신
날이고, 그 지적들이 전부 코드와 문서로 남았다.

### 1. 블로그 — 오진과 진짜 결함

**오진**: 09:25 이후 기록이 없다는 이유로 자동화가 죽었다고 단정했다. 실제로는
`blog-generate` 예약 창(09:00~09:25)이 끝난 것뿐이었다. 방아쇠는 **Supabase pg_cron**
인데 `docs/BLOG_OPS.md` 에는 아직 cron-job.org 라고 적혀 있었다 — 문서가 개편을 안 따라와
세 번째 오진을 만들었다.

**진짜 결함 4개를 찾아 고쳤다.**

| 결함 | 원인 | 조치 |
| --- | --- | --- |
| 오늘 편이 발행 보류 직전 | 검사식 오탐 — "이유는 무엇인가요?" 에 "때문입니다" 를 미달로 잡음 | `blog-audit.ts` 에 이유형 질문 예외 |
| 규격 미달이면 그날 편을 버림 | 패치 2회로 끝 | **REPAIR_LADDER** — 패치·재집필을 섞어 시간(15시)·돈($4) 안에서 채운다 |
| 편성표 날짜가 하루 밀림 | `kstMoment(...).toISOString().slice(0,10)` 이 전날을 준다 | `kstDate()` 신설, 작업표·원고 조회를 KST 로 |
| 생성비가 옆 회차 것으로 표시 | 위 날짜 밀림으로 join 이 어긋남 | 같은 수정으로 해결 |

**보강**: 검색 성과를 처음으로 **기록**하기 시작했다(`blog_search_daily`). 그전에는 화면을
열 때마다 구글에 물어보고 버렸다. 저녁 리포트에 **시스템 점검 5줄**을 붙여, 내가 없어도
매일 자가 점검이 돈다. 월 상한 $50 → $80(실측 월 $41 대비 여유가 없었다).

**남긴 것**: `docs/BLOG_CHECKLIST.md` + `scripts/blog-doctor.ts` (13항목 자동 점검).
문서를 믿지 말고 이걸 먼저 돌린다.

### 2. 광고영상 — 공정을 다시 세웠다

v10 의 결함(발음이 자막과 다름 / 얼굴이 깨짐 / 씬이 흐름에 안 맞음)은 전부
**"영상부터 만든다"** 에서 나왔다. 이미지 우선으로 뒤집었다.

```
① 상세페이지 링크 분석      scripts/adfilm-analyze.ts   ← 매번 건너뛰던 단계
② 소구 1개·컨셉 1개·통대본   drafts/*/concept-*.json
③ 씬 분할                  drafts/*/scenes-*.json
④ 캐릭터 시트 → 생성        adfilm-talent.mjs → adfilm-shoot-sora.mjs
⑤ 나레이션·자막 조립        adfilm-narrate.mjs → adfilm-assemble.mjs
```

**새로 만든 축 (전부 조합형. 고정 프리셋이 아니다)**

- `lib/adfilm-cast.ts` — 역할(구매자·수혜자·제공자·제품·화면·공간) × 발화(화면안·목소리만·무발화)
  × 발화구조(혼자·주고받기·한마디씩·무발화). **증거 컷 비중을 조합에서 계산**한다
- `lib/adfilm-arc.ts` — 훅 6종 / 클로징 6종. **귀결점(사면 해소된다) 검사**
- `lib/adfilm-settings.ts` — 템포·나레이션 밀도·문장 간격·자막 위치·줄수를 선택바로

**사장님이 가르쳐 주신 것** (`~/.claude/.../project_adfilm_planning_lessons.md` 에 기록)

1. 분석 없이 기획 없다. 상세페이지 링크가 유일한 입구다
2. 컷 수·길이를 먼저 정하고 내용을 자르지 않는다. 길이는 내용에서 나온다
3. 대사는 그 씬 화면에 보이는 것만 말한다
4. 개념어는 그 개념을 보여주는 컷이 앞에 있어야 쓸 수 있다
5. **원리 → 작동 → 효과 → 근거 → 안심** 이 없으면 "제품 좋으니 쓰래" 가 된다
6. 지적 하나에 대본을 통째로 다시 쓰지 않는다 — **최소 수정**
7. 실리뷰까지 확인해 검증 안 된 문장은 쓰지 않는다

### 3. 결과물

**펠리웨이 v16 — 1분 51초 완성본** (`drafts/feliway/feliway-v16.mp4`)

- 12씬 · 나레이션 28문장 · 자막 58장
- 나레이션 발음 검증 **28/28 통과**(whisper 일치율 99%), 8.0자/초
- 캐릭터 시트(사람 6장 + 고양이 6장 + 합성 1장)로 인물·고양이 일관성 유지
- 남은 결함: 제품 라벨이 AI 재생성이라 실물과 다름 / 1분 51초는 광고로 김(컷다운 필요) /
  자막 하이라이트 미적용

### 4. 인프라·과금

| 항목 | 상태 |
| --- | --- |
| Vercel | **Pro 전환 완료**(월 $20). Hobby 는 5분 cron 불가 + 상업 이용 약관 이슈였다 |
| Supabase | Pro 유지. 방아쇠(pg_cron)가 여기 있다 |
| **fal.ai** | ⚠️ **잔액 소진(403)**. Seedance 경로가 막혀 sora-2 로 우회했다 |
| sora-2 | `input_reference` 로 이미지 레퍼런스를 받는다(720x1280 정확히). $0.10/초 — Seedance 의 절반 이하 |
| ⚠️ sora-2 삭제 | **2026-09-24**. 그 전에 생성 층을 다시 갈아타야 한다 |
| 오늘 영상비 | sora 144초 ≈ **$14.4** + 이미지 13장 ≈ $2 |
| 보안 | `secret-scan.mjs` **유출 없음** (브라우저 파일 58 / 서버 키 14) |

### 5. 다음

1. 펠리웨이 **30·45초 컷다운** — 대본이 블록으로 나뉘어 있어 원리·근거를 덜면 나온다
2. 제품 컷을 **실물 합성**으로 교체 (규격: 제품은 AI 로 그리지 않는다)
3. fal.ai 충전 여부 결정 — sora-2 만으로 9/24 까지 버틸지, 지금 갈아탈지
4. 어드민에 정거장(분석 → 대본 → 씬 → 생성) 화면 심기. 지금은 CLI 로만 돈다
